---
phase: 02-layer-1-core-program
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/sss-1/src/constants.rs
  - programs/sss-1/src/state.rs
  - programs/sss-2/src/constants.rs
  - programs/sss-2/src/state.rs
autonomous: true
requirements: [CORE-01]
user_setup: []
must_haves:
  truths:
    - Stablecoin and minter PDAs use a seed scheme that remains valid after authority transfer.
    - Phase 2 defines one canonical shared account model for core lifecycle behavior without pulling Phase 3 compliance scope into the baseline.
    - Stored config and state fields are explicit about which flags are supported now versus deferred.
  artifacts:
    - programs/sss-1/src/constants.rs
    - programs/sss-1/src/state.rs
    - programs/sss-2/src/constants.rs
    - programs/sss-2/src/state.rs
  key_links:
    - Stablecoin seed constants align with the account constraints used by lifecycle instructions.
    - SSS-2 extends the shared baseline schema without changing the Phase 2 core contract surface.
---

<objective>
Lock the shared Layer 1 account model before deeper implementation work.

Purpose: Remove the current PDA and schema ambiguity so initialization, lifecycle logic, and tests all build on a stable contract surface.
Output: Finalized state structs, seed constants, and schema comments for the shared core program baseline.
</objective>

<execution_context>
@/home/abduh/.codex/get-shit-done/workflows/execute-plan.md
@/home/abduh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layer-1-core-program/02-CONTEXT.md
@.planning/phases/02-layer-1-core-program/02-RESEARCH.md
@programs/sss-1/src/constants.rs
@programs/sss-1/src/state.rs
@programs/sss-2/src/constants.rs
@programs/sss-2/src/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign stablecoin and minter PDA addressing</name>
  <files>programs/sss-1/src/constants.rs, programs/sss-1/src/state.rs, programs/sss-2/src/constants.rs, programs/sss-2/src/state.rs</files>
  <action>Replace any mutable-authority-coupled PDA assumptions with an immutable addressing strategy that still works after authority transfer. Update the seed comments, bump expectations, and struct docs in both programs together so the shared account model cannot drift again. Do not add compliance-only PDAs or Phase 3 behavior here.</action>
  <verify>anchor build</verify>
  <done>Both programs compile with one coherent stablecoin PDA and minter PDA strategy, and the state comments make the new contract surface explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the shared Phase 2 schema boundary</name>
  <files>programs/sss-1/src/state.rs, programs/sss-2/src/state.rs</files>
  <action>Canonicalize `StablecoinConfig`, `Stablecoin`, and `MinterConfig` so Phase 2 clearly owns the shared lifecycle fields and defers compliance-only behavior cleanly. Make an intentional decision about `default_account_frozen` and unsupported extension flags so later plans do not inherit ambiguous fields or comments.</action>
  <verify>anchor build</verify>
  <done>The shared core schema is explicit, phase-scoped, and ready for initialization logic without hidden deferred behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `anchor build`
- [ ] Seed and state comments match the actual account model used by both programs
- [ ] No Phase 3-only compliance behavior was added to the Phase 2 baseline
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The core account model is stable enough for initialization and admin logic to build on
</success_criteria>

<output>
After completion, create `.planning/phases/02-layer-1-core-program/02-01-SUMMARY.md`
</output>
