---
phase: 02-layer-1-core-program
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - programs/sss-1/src/instructions/initialize.rs
  - programs/sss-1/src/lib.rs
  - programs/sss-1/src/events.rs
  - programs/sss-1/src/error.rs
  - programs/sss-2/src/instructions/initialize.rs
  - programs/sss-2/src/lib.rs
  - programs/sss-2/src/events.rs
  - programs/sss-2/src/error.rs
autonomous: true
requirements: [CORE-01, CORE-02]
user_setup: []
must_haves:
  truths:
    - `initialize` creates and configures a real Token-2022 mint instead of only storing a pubkey.
    - Mint authority and freeze authority are assigned consistently with the stablecoin PDA model chosen in Plan 01.
    - Unsupported or invalid config flags fail through explicit Anchor errors.
  artifacts:
    - programs/sss-1/src/instructions/initialize.rs
    - programs/sss-2/src/instructions/initialize.rs
    - programs/sss-1/src/error.rs
    - programs/sss-2/src/error.rs
  key_links:
    - Initialization persists state that matches the finalized Phase 2 schema from Plan 01.
    - SSS-2 initializer stays compatible with the shared core contract without implementing transfer-hook behavior here.
---

<objective>
Implement real Layer 1 initialization around the finalized Phase 2 schema.

Purpose: Make the shared contract usable by creating a real Token-2022 mint, assigning the right authorities, and persisting stable state that later flows can trust.
Output: Working `initialize` handlers, explicit initialization errors, and parity-safe entrypoint wiring in both programs.
</objective>

<execution_context>
@/home/abduh/.codex/get-shit-done/workflows/execute-plan.md
@/home/abduh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layer-1-core-program/02-CONTEXT.md
@.planning/phases/02-layer-1-core-program/02-RESEARCH.md
@.planning/phases/02-layer-1-core-program/02-01-SUMMARY.md
@programs/sss-1/src/instructions/initialize.rs
@programs/sss-1/src/lib.rs
@programs/sss-1/src/events.rs
@programs/sss-1/src/error.rs
@programs/sss-2/src/instructions/initialize.rs
@programs/sss-2/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement real Token-2022 mint creation for SSS-1</name>
  <files>programs/sss-1/src/instructions/initialize.rs, programs/sss-1/src/lib.rs, programs/sss-1/src/events.rs, programs/sss-1/src/error.rs</files>
  <action>Rewrite `sss-1` initialization so it creates and validates the intended Token-2022 mint account, assigns the correct authorities, persists the finalized state model, and rejects unsupported config combinations with explicit errors. Keep the implementation anchored to the Phase 2 boundary rather than adding compliance-only extension behavior.</action>
  <verify>anchor build</verify>
  <done>`sss-1` initialization creates a real, correctly configured mint and stores state that later lifecycle instructions can use safely.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the minimum required initializer parity into SSS-2</name>
  <files>programs/sss-2/src/instructions/initialize.rs, programs/sss-2/src/lib.rs, programs/sss-2/src/events.rs, programs/sss-2/src/error.rs</files>
  <action>Update the `sss-2` initializer so the shared Layer 1 contract surface remains aligned with SSS-1 while still deferring transfer-hook enforcement and blacklist behavior to Phase 3. Preserve only the minimum config validation needed for the shared core and fail clearly when unsupported states are requested.</action>
  <verify>anchor build</verify>
  <done>`sss-2` stays aligned with the finalized Phase 2 initialization contract without collapsing the boundary into compliance implementation work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `anchor build`
- [ ] `initialize` creates a real Token-2022 mint and persists the corresponding stablecoin state
- [ ] Unsupported flags and invalid config paths fail through explicit Anchor errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Initialization becomes a trustworthy foundation for lifecycle/admin flows
</success_criteria>

<output>
After completion, create `.planning/phases/02-layer-1-core-program/02-02-SUMMARY.md`
</output>
