---
phase: 02-layer-1-core-program
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - Anchor.toml
  - tests/helpers/index.ts
  - tests/sss-1.ts
  - tests/integration.ts
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03]
user_setup: []
must_haves:
  truths:
    - `tests/sss-1.ts` becomes the authoritative automated proof for Phase 2 happy-path and negative-path behavior.
    - Test assertions cover PDA derivation, state persistence, mint authority wiring, pause behavior, quotas, and authorization failures.
    - The repo has a runnable targeted validation entry point for the Layer 1 contract.
  artifacts:
    - tests/sss-1.ts
    - tests/helpers/index.ts
    - Anchor.toml
  key_links:
    - The test suite exercises the exact initialization and lifecycle flows hardened in Plans 02 and 03.
    - Test helpers derive the same PDAs and authorities that the on-chain contract now expects.
---

<objective>
Turn the Phase 2 contract into a verifiable deliverable through targeted Anchor integration tests.

Purpose: Replace the current TODO scaffolding with an authoritative automated suite that proves the Layer 1 contract actually works and fails correctly.
Output: Runnable Phase 2 integration tests, helper coverage for account setup, and any minimal repo plumbing needed for targeted execution.
</objective>

<execution_context>
@/home/abduh/.codex/get-shit-done/workflows/execute-plan.md
@/home/abduh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layer-1-core-program/02-CONTEXT.md
@.planning/phases/02-layer-1-core-program/02-RESEARCH.md
@.planning/phases/02-layer-1-core-program/02-VALIDATION.md
@.planning/phases/02-layer-1-core-program/02-02-SUMMARY.md
@.planning/phases/02-layer-1-core-program/02-03-SUMMARY.md
@Anchor.toml
@tests/helpers/index.ts
@tests/sss-1.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the targeted Layer 1 test entry point runnable</name>
  <files>Anchor.toml, tests/helpers/index.ts, tests/integration.ts</files>
  <action>Apply the minimal repo plumbing needed so Phase 2 has a reliable test harness. If the current root test wiring or helper coverage blocks targeted Anchor execution, fix that here with the smallest possible surface area. Do not broaden the suite into full SSS-2 or later-phase coverage just to make commands look greener.</action>
  <verify>yarn test:sss1</verify>
  <done>The repository has a runnable targeted command for Layer 1 validation and the helper layer supports the required fixtures and PDA derivations.</done>
</task>

<task type="auto">
  <name>Task 2: Replace SSS-1 TODOs with authoritative happy-path coverage</name>
  <files>tests/sss-1.ts, tests/helpers/index.ts</files>
  <action>Implement the happy-path suite for initialize, role defaults, update_minter, mint, freeze, thaw, burn, pause, and unpause using the finalized Phase 2 contract surface. Assertions must inspect account state and mint configuration, not only transaction success.</action>
  <verify>yarn test:sss1</verify>
  <done>`tests/sss-1.ts` proves the expected happy-path Layer 1 behavior against a real Token-2022 mint and stablecoin state.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit negative-path and account-model assertions</name>
  <files>tests/sss-1.ts</files>
  <action>Add negative-path checks for zero amounts, quota overflow, paused operations, unauthorized operators, and authority-transfer/admin edge cases. Include direct assertions for stablecoin PDA derivation, minter PDA derivation, persisted flags, and mint authority/freeze authority ownership so Phase 2 cannot claim success on transaction results alone.</action>
  <verify>yarn test:sss1</verify>
  <done>The targeted suite proves both success and failure behavior for CORE-01 through CORE-03 and acts as the authoritative phase validator.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn test:sss1`
- [ ] `anchor build`
- [ ] Test assertions cover PDA derivation, state persistence, authorization, pause, and quota behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a real automated proof path instead of TODO-only tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-layer-1-core-program/02-04-SUMMARY.md`
</output>
