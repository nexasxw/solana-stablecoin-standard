---
phase: 03-compliance-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/sss-transfer-hook/src/instructions/transfer_hook.rs
  - docs/ARCHITECTURE.md
  - docs/SSS-2.md
autonomous: true
requirements: [COMP-01, COMP-03]
user_setup: []
must_haves:
  truths:
    - Token-2022 transfers for SSS-2 mints always run the transfer hook and reject blacklisted sender/recipient wallets.
    - The transfer hook only enforces blacklist behavior when the referenced SSS-2 stablecoin PDA is initialized (prevents external mints being griefed by spoofed blacklist PDAs).
    - Hook behavior remains stable and explicit via deterministic errors and safe “missing PDA means not blacklisted” handling.
  artifacts:
    - programs/sss-transfer-hook/src/instructions/transfer_hook.rs
  key_links:
    - Extra account metas resolution stays consistent with the hook account struct (stablecoin PDA + sender/recipient blacklist PDAs).
    - Docs describe the real enforcement boundary and do not claim a different seize/seed model than code implements.
---

<objective>
Harden and clarify the transfer-hook enforcement path.

Purpose: Ensure “every transfer, no gaps” for SSS-2 without making the hook unsafe for non-SSS-2 mints.
Output: A transfer hook handler that only enforces blacklist rules when an initialized SSS-2 stablecoin state exists, plus docs alignment for the enforcement story.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-compliance-module/03-CONTEXT.md
@.planning/phases/03-compliance-module/03-RESEARCH.md
@programs/sss-transfer-hook/src/instructions/transfer_hook.rs
@programs/sss-transfer-hook/src/instructions/initialize.rs
@programs/sss-2/src/instructions/initialize.rs
@docs/ARCHITECTURE.md
@docs/SSS-2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate hook enforcement on initialized SSS-2 stablecoin state</name>
  <files>programs/sss-transfer-hook/src/instructions/transfer_hook.rs</files>
  <action>Add a defensive guard so blacklist checks only run when the passed stablecoin PDA account is owned by the SSS-2 program and has non-empty data. If the stablecoin account is missing/uninitialized (e.g. non-SSS-2 mint pointing at this hook), return Ok without rejecting transfers. Preserve the “missing blacklist PDA means not blacklisted” behavior.</action>
  <verify>anchor build</verify>
  <done>The hook rejects blacklisted wallets for real SSS-2 mints but remains safe/no-op for non-SSS-2 mints.</done>
</task>

<task type="auto">
  <name>Task 2: Align docs with the real hook enforcement boundary</name>
  <files>docs/SSS-2.md, docs/ARCHITECTURE.md</files>
  <action>Update the compliance docs to match the actual hook enforcement boundary (stablecoin must exist) and ensure any PDA seed references used by the hook story are consistent with the mint-derived stablecoin model.</action>
  <verify>yarn test:sss2</verify>
  <done>Docs no longer suggest an enforcement model that differs from the implemented hook path, and the SSS-2 integration test remains green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `anchor build`
- [ ] `yarn test:sss2`
- [ ] Transfer-hook rejection cases still fail with explicit hook errors
- [ ] Non-SSS-2 mint behavior is unaffected by hook enforcement changes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- COMP-01 and COMP-03 are satisfied by provable transfer-hook behavior
</success_criteria>

<output>
After completion, create .planning/phases/03-compliance-module/03-01-SUMMARY.md
</output>
