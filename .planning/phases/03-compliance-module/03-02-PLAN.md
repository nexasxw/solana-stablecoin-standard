---
phase: 03-compliance-module
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/sss-2/src/instructions/compliance.rs
  - programs/sss-2/src/error.rs
  - programs/sss-2/src/lib.rs
  - sdk/core/src/compliance.ts
  - tests/sss-2.ts
autonomous: true
requirements: [COMP-02, COMP-03]
user_setup: []
must_haves:
  truths:
    - Seizure requires: designated treasury configured, target wallet blacklisted, and target token account frozen.
    - Seizure eligibility is validated against the correct blacklist PDA for the target wallet (cannot be spoofed with an unrelated initialized account).
    - SDK helpers pass all required accounts for compliance instructions and match the on-chain account model.
  artifacts:
    - programs/sss-2/src/instructions/compliance.rs
    - sdk/core/src/compliance.ts
    - tests/sss-2.ts
  key_links:
    - The blacklist PDA is wallet-based and is derived consistently across SSS-2 program, transfer hook, tests, and SDK.
    - `seize` remains full-balance-only and always targets the stablecoin’s configured treasury token account.
---

<objective>
Lock down the SSS-2 compliance workflow (blacklist + seizure) and align the SDK surface.

Purpose: Ensure the on-chain compliance model cannot be bypassed or spoofed via flexible accounts, and keep the SDK consistent with the finalized instruction accounts.
Output: Hardened `seize` account constraints + matching SDK/test updates.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-compliance-module/03-CONTEXT.md
@.planning/phases/03-compliance-module/03-RESEARCH.md
@programs/sss-2/src/instructions/compliance.rs
@sdk/core/src/compliance.ts
@tests/sss-2.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make seize validate the correct blacklist PDA for the target wallet</name>
  <files>programs/sss-2/src/instructions/compliance.rs, programs/sss-2/src/error.rs</files>
  <action>Adjust `Seize` accounts so the program proves the target wallet is blacklisted using the canonical PDA derivation. Prefer explicit, checkable constraints (e.g. include a target owner account and seed the blacklist PDA from it) rather than accepting any initialized account. Preserve the locked seizure policy: must be blacklisted + frozen, full-balance only, and treasury must match the configured stablecoin treasury.</action>
  <verify>anchor build</verify>
  <done>It is impossible to satisfy seize’s “blacklisted” precondition by passing an unrelated initialized account; only the target wallet’s blacklist PDA works.</done>
</task>

<task type="auto">
  <name>Task 2: Align SDK and tests with the finalized seize accounts</name>
  <files>sdk/core/src/compliance.ts, tests/sss-2.ts, programs/sss-2/src/lib.rs</files>
  <action>Update the SDK compliance module to pass all required accounts for `seize` (including the derived blacklist PDA for the target wallet if required by Task 1). Update `tests/sss-2.ts` to match the new account list and keep all negative-path seizure checks intact.</action>
  <verify>yarn test:sss2</verify>
  <done>The SSS-2 integration suite passes, and the SDK’s `seize` helper matches the on-chain interface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `anchor build`
- [ ] `yarn test:sss2`
- [ ] Seizure still rejects: treasury missing, not frozen, not blacklisted
- [ ] Seizure happy path seizes full balance to configured treasury
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- COMP-02 and COMP-03 are satisfied with spoof-resistant on-chain checks
</success_criteria>

<output>
After completion, create .planning/phases/03-compliance-module/03-02-SUMMARY.md
</output>
