---
phase: 03-compliance-module
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - sdk/core/src/pda.ts
  - docs/ARCHITECTURE.md
  - docs/SSS-2.md
  - CLAUDE.md
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
user_setup: []
must_haves:
  truths:
    - SDK PDA derivations match the on-chain mint-derived stablecoin PDA model used by SSS-2 and tests.
    - Compliance docs describe the real seize mechanics and stablecoin seed model (no “transfer_checked” or authority-seeded drift).
    - Downstream consumers (SDK/docs) do not encode assumptions that would break SSS-2 compliance flows.
  artifacts:
    - sdk/core/src/pda.ts
    - docs/ARCHITECTURE.md
    - docs/SSS-2.md
    - CLAUDE.md
  key_links:
    - SDK, docs, and tests derive stablecoin and blacklist PDAs consistently.
    - Documented compliance flows reflect the exact account and CPI mechanics used on-chain.
---

<objective>
Eliminate SDK/docs drift around SSS-2 compliance.

Purpose: Phase 3 is only “done” if downstream references (SDK + docs) describe and derive the same PDAs and behavior the programs enforce.
Output: Updated SDK PDA helpers and corrected documentation for seeds and seize mechanics.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-compliance-module/03-CONTEXT.md
@.planning/phases/03-compliance-module/03-RESEARCH.md
@sdk/core/src/pda.ts
@docs/ARCHITECTURE.md
@docs/SSS-2.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SDK stablecoin PDA derivation to mint-derived addressing</name>
  <files>sdk/core/src/pda.ts</files>
  <action>Change the SDK’s stablecoin PDA derivation to match the on-chain mint-derived stablecoin PDA model used by the programs and tests. Keep naming and ergonomics consistent with existing SDK style, and ensure blacklist PDA derivation remains wallet-based (stablecoin + address).</action>
  <verify>yarn build</verify>
  <done>SDK PDA helpers match the program’s seed scheme; no consumer would derive an authority-seeded stablecoin address for SSS-2.</done>
</task>

<task type="auto">
  <name>Task 2: Fix docs/CLAUDE references that still describe outdated seeds or seize mechanics</name>
  <files>docs/ARCHITECTURE.md, docs/SSS-2.md, CLAUDE.md</files>
  <action>Update documentation that still claims stablecoin PDAs are authority-seeded or that seize is implemented as a Token-2022 transfer that would be blocked by the hook. Ensure docs accurately describe the actual seize mechanics and the “stablecoin must be initialized to enforce” hook boundary.</action>
  <verify>yarn test:sss2</verify>
  <done>Docs and contributor guidance match the shipped code behavior for compliance flows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn build`
- [ ] `yarn test:sss2`
- [ ] Docs no longer reference authority-derived stablecoin PDA seeds
- [ ] Docs no longer claim seize is a transfer-hook-covered transfer
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 3 downstream references are aligned with the on-chain compliance implementation
</success_criteria>

<output>
After completion, create .planning/phases/03-compliance-module/03-03-SUMMARY.md
</output>
