---
phase: 04-preset-configurations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/core/src/presets.ts
  - sdk/core/src/config.ts
  - sdk/core/src/stablecoin.ts
  - sdk/core/src/types.ts
  - sdk/core/tests/presets.test.ts
  - sdk/core/tests/config.test.ts
  - sdk/core/tests/stablecoin.create.test.ts
autonomous: true
requirements: [PRE-01, PRE-02, PRE-03]
user_setup: []
must_haves:
  truths:
    - The SDK exposes canonical SSS-1 and SSS-2 presets with locked extension expectations (SSS-1: both false, SSS-2: both true) and `defaultAccountFrozen = false` for shipped presets.
    - Custom TOML and JSON config inputs are normalized to runtime `StablecoinConfig` through strict validation (required name/symbol, unknown-field rejection, defaults, and paired compliance flags).
    - Final initialization config resolution remains deterministic as `explicit options > config file > preset defaults` and is enforced on the `create` path.
  artifacts:
    - sdk/core/src/presets.ts
    - sdk/core/src/config.ts
    - sdk/core/src/stablecoin.ts
    - sdk/core/tests/presets.test.ts
    - sdk/core/tests/config.test.ts
    - sdk/core/tests/stablecoin.create.test.ts
  key_links:
    - Preset constants, parser/validator logic, and `SolanaStablecoin.create` compatibility guards encode one consistent preset/config contract.
    - SDK tests prove helper-level and create-path behavior for strict validation, compatibility failures, and merge precedence.
---

<objective>
Deliver the executable SDK contract for presets and custom config resolution.

Purpose: Complete the code and tests that enforce PRE-01/02/03 behavior in SDK core before docs closeout.
Output: Hardened preset/config code path with create-focused regression coverage.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-preset-configurations/04-CONTEXT.md
@.planning/phases/04-preset-configurations/04-RESEARCH.md
@sdk/core/src/presets.ts
@sdk/core/src/config.ts
@sdk/core/src/stablecoin.ts
@sdk/core/src/types.ts
@sdk/core/tests/presets.test.ts
@sdk/core/tests/config.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock preset contract and create-path compatibility checks</name>
  <files>sdk/core/src/presets.ts, sdk/core/src/stablecoin.ts, sdk/core/src/types.ts</files>
  <action>Ensure SSS-1 and SSS-2 preset behavior is explicit and defensive on the runtime create path: SSS-1 must reject compliance-enabled overrides, SSS-2 must reject compliance-disabled overrides, and unsupported preset values must not silently resolve.</action>
  <verify>yarn test:sdk</verify>
  <done>Preset selection and compatibility checks enforce the exact SSS-1/SSS-2 contract from phase context.</done>
</task>

<task type="auto">
  <name>Task 2: Harden strict TOML/JSON parsing, validation, and precedence</name>
  <files>sdk/core/src/config.ts, sdk/core/tests/config.test.ts</files>
  <action>Confirm and harden strict config behavior: snake_case file keys, unknown-field rejection, required non-empty name/symbol, defaults (`uri`, `decimals`, `default_account_frozen`), paired compliance flags, extension-based format detection with optional override, and merge order `explicit > file > preset`.</action>
  <verify>yarn test:sdk</verify>
  <done>Custom config handling is deterministic and strict for both TOML and JSON inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Add create-focused regression coverage for preset/config integration</name>
  <files>sdk/core/tests/stablecoin.create.test.ts, sdk/core/tests/presets.test.ts, sdk/core/tests/config.test.ts</files>
  <action>Add or extend SDK tests that exercise `SolanaStablecoin.create` with presets, config files, and explicit overrides, including negative compatibility paths and precedence assertions. Keep tests deterministic and independent of network behavior.</action>
  <verify>yarn test:sdk</verify>
  <done>Coverage proves preset/config behavior at both helper and create integration levels.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn test:sdk`
- [ ] Preset compatibility tests fail for invalid SSS-1/SSS-2 flag combinations
- [ ] Config parsing rejects unknown TOML/JSON keys and mixed compliance flags
- [ ] Merge precedence remains `explicit > file > preset`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- PRE-01, PRE-02, and PRE-03 are satisfied at SDK code/test contract level
</success_criteria>

<output>
After completion, create .planning/phases/04-preset-configurations/04-01-SUMMARY.md
</output>
