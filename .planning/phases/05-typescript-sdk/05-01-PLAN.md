---
phase: 05-typescript-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/core/src/stablecoin.ts
  - sdk/core/src/types.ts
  - sdk/core/src/index.ts
  - sdk/core/src/errors.ts
  - sdk/core/src/client.ts
  - sdk/core/tests/stablecoin.create.test.ts
autonomous: true
requirements: [SDK-01, SDK-02, SDK-03]
user_setup: []
must_haves:
  truths:
    - `SolanaStablecoin.create` and `SolanaStablecoin.load` expose a typed contract that preserves config precedence (`explicit > file > preset`) and deterministic SSS-1/SSS-2 variant resolution.
    - SDK mutation contracts use one shared transaction result envelope and typed error codes so downstream CLI/services can branch on machine-readable failures.
    - Compliance surface availability is stateful and explicit (`compliance` only when SSS-2 compatibility flags are enabled).
  artifacts:
    - sdk/core/src/stablecoin.ts
    - sdk/core/src/types.ts
    - sdk/core/src/errors.ts
    - sdk/core/src/client.ts
    - sdk/core/src/index.ts
    - sdk/core/tests/stablecoin.create.test.ts
  key_links:
    - `create/load` variant handling and config merge behavior are validated by deterministic SDK tests.
    - Shared tx result and error contracts become the canonical boundary reused by lifecycle and compliance plans.
---

<objective>
Establish the Phase 5 SDK contract foundation before instruction-specific implementation.

Purpose: Lock API primitives and initialization/load behavior that all later SDK operations depend on.
Output: Typed create/load, typed errors, and shared tx-result contract with regression coverage.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-typescript-sdk/05-CONTEXT.md
@.planning/phases/05-typescript-sdk/05-RESEARCH.md
@.planning/phases/05-typescript-sdk/05-VALIDATION.md
@CLAUDE.md
@sdk/core/src/stablecoin.ts
@sdk/core/src/types.ts
@sdk/core/src/index.ts
@sdk/core/src/config.ts
@sdk/core/src/presets.ts
@sdk/core/tests/stablecoin.create.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical SDK result and error contracts</name>
  <files>sdk/core/src/types.ts, sdk/core/src/errors.ts, sdk/core/src/index.ts</files>
  <action>Introduce typed SDK primitives for transaction mutation responses (signature + confirmation metadata) and stable machine-readable error codes/classes covering validation failures, unsupported operations, and wrapped RPC errors. Export these contracts as public SDK surface.</action>
  <verify>yarn test:sdk</verify>
  <done>Core SDK result/error types are stable, exported, and enforced by tests/compilation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed client resolution and create/load contracts</name>
  <files>sdk/core/src/client.ts, sdk/core/src/stablecoin.ts</files>
  <action>Replace placeholder program wiring with an IDL/type-driven client factory and implement `create` + `load` contracts to preserve preset/config precedence, deterministic variant resolution, and explicit typed failure paths. Ensure `create()` returns initialized SDK instance plus initialization transaction metadata per phase context.</action>
  <verify>yarn test:sdk</verify>
  <done>Create/load behavior is deterministic, typed, and no longer depends on TODO placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Add initialization/load regression coverage</name>
  <files>sdk/core/tests/stablecoin.create.test.ts</files>
  <action>Extend SDK tests to assert `create/load` return shapes, variant gating, config precedence behavior, and stable SDK error codes for local preflight failures without relying on network flakiness.</action>
  <verify>yarn test:sdk</verify>
  <done>Tests prove SDK-01 contract behavior and protect foundation assumptions for later plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn test:sdk`
- [ ] `create()` returns SDK instance + initialization transaction metadata
- [ ] `load()` variant behavior is deterministic and validated
- [ ] Public exports include tx result types and stable SDK error contracts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- SDK-01 is materially satisfied and SDK-02/SDK-03 foundational contracts are in place
</success_criteria>

<output>
After completion, create .planning/phases/05-typescript-sdk/05-01-SUMMARY.md
</output>
