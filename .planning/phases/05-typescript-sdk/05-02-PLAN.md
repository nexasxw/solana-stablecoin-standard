---
phase: 05-typescript-sdk
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - sdk/core/src/stablecoin.ts
  - sdk/core/src/types.ts
  - sdk/core/src/errors.ts
  - sdk/core/tests/stablecoin.lifecycle.test.ts
  - sdk/core/tests/stablecoin.create.test.ts
autonomous: true
requirements: [SDK-01, SDK-02, SDK-03]
user_setup: []
must_haves:
  truths:
    - Lifecycle operations map directly to the on-chain instruction surface with explicit role signers and no hidden authority assumptions.
    - Amount-bearing APIs use `bigint` as the canonical public contract with strict u64 preflight checks before RPC submission.
    - Mutating lifecycle methods return the shared typed transaction result envelope and normalize local/RPC failures into stable SDK error codes.
  artifacts:
    - sdk/core/src/stablecoin.ts
    - sdk/core/src/types.ts
    - sdk/core/src/errors.ts
    - sdk/core/tests/stablecoin.lifecycle.test.ts
  key_links:
    - Lifecycle API behavior is contract-tested with signer requirements, amount validation, and typed response/error invariants.
    - Instruction mapping stays aligned to generated IDL client calls rather than manual serializer logic.
---

<objective>
Implement the full typed lifecycle API surface on top of the Phase 5 foundation.

Purpose: Satisfy SDK-02 while preserving the explicit signer and bigint contracts locked in phase context.
Output: Runtime lifecycle methods and regression tests aligned to Layer 1 instruction behavior.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-typescript-sdk/05-CONTEXT.md
@.planning/phases/05-typescript-sdk/05-RESEARCH.md
@.planning/phases/05-typescript-sdk/05-VALIDATION.md
@CLAUDE.md
@.planning/phases/05-typescript-sdk/05-01-SUMMARY.md
@sdk/core/src/stablecoin.ts
@sdk/core/src/types.ts
@sdk/core/src/errors.ts
@sdk/core/src/pda.ts
@sdk/core/tests/stablecoin.create.test.ts
@tests/sss-1.ts
@tests/sss-2.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement typed lifecycle mutation methods</name>
  <files>sdk/core/src/stablecoin.ts, sdk/core/src/types.ts</files>
  <action>Implement `mint`, `burn`, `freeze`, `thaw`, `pause`, `unpause`, role/admin update operations, and state-fetch helpers using generated Anchor clients. Require explicit signer parameters for role-gated methods and return shared typed transaction result envelopes for every mutation.</action>
  <verify>yarn test:sdk</verify>
  <done>Lifecycle API parity with on-chain contract exists through typed SDK methods.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce bigint/u64 preflight and typed error normalization</name>
  <files>sdk/core/src/stablecoin.ts, sdk/core/src/errors.ts</files>
  <action>Enforce `bigint` amount boundaries (including u64 range and non-negative checks), signer/input invariants, and deterministic local preflight failures through stable SDK error codes. Wrap RPC failures into typed SDK error variants without introducing implicit retries.</action>
  <verify>yarn test:sdk</verify>
  <done>Lifecycle methods fail predictably with machine-readable error codes for preflight and RPC paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add lifecycle regression tests</name>
  <files>sdk/core/tests/stablecoin.lifecycle.test.ts, sdk/core/tests/stablecoin.create.test.ts</files>
  <action>Add focused SDK tests for lifecycle signatures, explicit signer enforcement, bigint validation, and tx-result envelope consistency. Keep deterministic mocks/stubs for local invariants and avoid flaky network assumptions.</action>
  <verify>yarn test:sdk</verify>
  <done>SDK-02 behavior is guarded by reproducible test coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn test:sdk`
- [ ] Lifecycle mutating calls return typed transaction result envelope (not signature strings)
- [ ] Amount-bearing APIs reject non-`bigint`/invalid u64 inputs with stable SDK error codes
- [ ] Role-gated methods require explicit per-call signers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- SDK-02 is satisfied with typed, validated lifecycle API parity
</success_criteria>

<output>
After completion, create .planning/phases/05-typescript-sdk/05-02-SUMMARY.md
</output>
