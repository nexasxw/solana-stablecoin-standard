---
phase: 05-typescript-sdk
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - sdk/core/src/compliance.ts
  - sdk/core/src/errors.ts
  - sdk/core/src/types.ts
  - sdk/core/src/index.ts
  - sdk/core/tests/compliance.test.ts
autonomous: true
requirements: [SDK-01, SDK-02, SDK-03]
user_setup: []
must_haves:
  truths:
    - Compliance helpers are available only for SSS-2-compatible deployments and surface explicit unsupported-operation failures otherwise.
    - `blacklistAdd` enforces non-empty trimmed reason preflight and compliance mutations require explicit operator signers.
    - `seize` preserves explicit account contract (source token account, target owner, treasury token account) and returns shared typed transaction result metadata.
  artifacts:
    - sdk/core/src/compliance.ts
    - sdk/core/src/errors.ts
    - sdk/core/src/types.ts
    - sdk/core/src/index.ts
    - sdk/core/tests/compliance.test.ts
  key_links:
    - Compliance module preflight and RPC behavior share the same typed error/result contracts as lifecycle APIs.
    - Test coverage proves SDK-03 workflows and rejects invalid reason/signer/account inputs early.
---

<objective>
Deliver SSS-2 compliance helper APIs with explicit operator and account contracts.

Purpose: Satisfy SDK-03 with robust preflight validation and typed behavior consistent with the rest of the SDK.
Output: Hardened compliance module and regression tests for blacklist/seize workflows.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-typescript-sdk/05-CONTEXT.md
@.planning/phases/05-typescript-sdk/05-RESEARCH.md
@.planning/phases/05-typescript-sdk/05-VALIDATION.md
@CLAUDE.md
@.planning/phases/05-typescript-sdk/05-01-SUMMARY.md
@sdk/core/src/compliance.ts
@sdk/core/src/errors.ts
@sdk/core/src/types.ts
@sdk/core/src/pda.ts
@sdk/core/src/index.ts
@tests/sss-2.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden compliance module contracts and gating behavior</name>
  <files>sdk/core/src/compliance.ts, sdk/core/src/types.ts</files>
  <action>Implement typed compliance mutation responses, enforce explicit operator signer inputs per call, and guarantee SSS-2-only availability semantics. Preserve explicit seizure account tuple parameters (`fromTokenAccount`, `targetOwner`, `treasuryTokenAccount`) with no implicit account derivation that changes contract clarity.</action>
  <verify>yarn test:sdk</verify>
  <done>Compliance helper surface matches locked Phase 5 operator/account contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add preflight validation and stable compliance error codes</name>
  <files>sdk/core/src/compliance.ts, sdk/core/src/errors.ts</files>
  <action>Add local invariant checks (including non-empty trimmed reason for `blacklistAdd`) and map all compliance local/RPC failures into stable machine-readable SDK error codes aligned with shared error taxonomy.</action>
  <verify>yarn test:sdk</verify>
  <done>Compliance flows fail predictably with typed, branchable error semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add compliance-focused regression tests</name>
  <files>sdk/core/tests/compliance.test.ts, sdk/core/src/index.ts</files>
  <action>Create deterministic tests for blacklist add/remove/seize method contracts, SSS-1 inaccessibility behavior, reason validation, explicit signer requirements, and typed transaction result/error shapes.</action>
  <verify>yarn test:sdk</verify>
  <done>SDK-03 behavior is covered by reproducible tests and exported API surface checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn test:sdk`
- [ ] Compliance helpers are inaccessible on non-SSS-2 deployments
- [ ] `blacklistAdd` rejects empty/whitespace-only reasons preflight
- [ ] `seize` contract keeps explicit account tuple and returns typed tx-result metadata
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- SDK-03 is satisfied with typed, validated SSS-2 compliance helper workflows
</success_criteria>

<output>
After completion, create .planning/phases/05-typescript-sdk/05-03-SUMMARY.md
</output>
