---
phase: 06-admin-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/core/src/stablecoin.ts
  - sdk/core/src/cli.ts
  - sdk/core/src/cli/config.ts
  - sdk/core/src/cli/context.ts
  - sdk/core/src/cli/output.ts
  - sdk/core/src/cli/errors.ts
  - sdk/core/src/cli/signer.ts
  - sdk/core/src/cli/types.ts
  - sdk/core/tests/stablecoin.create.test.ts
  - sdk/core/tests/cli.config.test.ts
  - sdk/core/tests/cli.output.test.ts
autonomous: true
requirements: [CLI-01, CLI-03]
user_setup: []
must_haves:
  truths:
    - CLI bootstrap exists with deterministic command wiring, strict non-zero failure exits, and stable machine-readable JSON output mode.
    - Runtime config resolution is canonical and test-guarded as `flags > environment variables > config file` with strict validation errors.
    - CLI context and signer resolution are explicit, enabling per-command override with global defaults and no hidden signer inference.
  artifacts:
    - sdk/core/src/stablecoin.ts
    - sdk/core/src/cli.ts
    - sdk/core/src/cli/config.ts
    - sdk/core/src/cli/context.ts
    - sdk/core/src/cli/output.ts
    - sdk/core/src/cli/errors.ts
    - sdk/core/src/cli/signer.ts
    - sdk/core/tests/stablecoin.create.test.ts
    - sdk/core/tests/cli.config.test.ts
    - sdk/core/tests/cli.output.test.ts
  key_links:
    - Foundation contracts established here are reused by all lifecycle and compliance command handlers in later plans.
    - This plan includes a phase-entry SDK initialization realism gate to avoid shipping CLI init over a simulated backend path.
---

<objective>
Deliver the CLI foundation and runtime configuration layer that all Phase 6 commands depend on.

Purpose: Create executable CLI bootstrap, deterministic config/env/flag precedence, signer/context loading, and output/error contracts.
Output: A stable CLI core that downstream plans can extend without reworking configuration or error semantics.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-cli/06-CONTEXT.md
@.planning/phases/06-admin-cli/06-RESEARCH.md
@.planning/phases/06-admin-cli/06-VALIDATION.md
@CLAUDE.md
@README.md
@sdk/core/package.json
@sdk/core/src/stablecoin.ts
@sdk/core/src/config.ts
@sdk/core/src/errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phase-entry gate for SDK initialization realism</name>
  <files>sdk/core/src/stablecoin.ts, sdk/core/tests/stablecoin.create.test.ts</files>
  <action>Validate whether `SolanaStablecoin.create()` submits a real initialize transaction. If still simulated, implement/fix the initialization RPC path before CLI init wiring. Update tests to assert non-simulated initialization behavior and typed tx metadata invariants.</action>
  <verify>yarn test:sdk</verify>
  <done>SDK init path is real and suitable for operator CLI init workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CLI bootstrap and shared runtime context</name>
  <files>sdk/core/src/cli.ts, sdk/core/src/cli/context.ts, sdk/core/src/cli/types.ts</files>
  <action>Create the top-level `sss-token` commander program with global flags (`--config`, `--rpc-url`, `--mint`, `--variant`, `--json`, `--yes`) and domain command groups. Build a shared command context loader that constructs SDK connection/runtime defaults and enforces required context per command.</action>
  <verify>yarn workspace @stbr/sss-token build</verify>
  <done>CLI entrypoint is executable and provides a reusable context contract for all subcommands.</done>
</task>

<task type="auto">
  <name>Task 3: Build deterministic runtime config, env, and signer resolution</name>
  <files>sdk/core/src/cli/config.ts, sdk/core/src/cli/signer.ts, sdk/core/src/cli/errors.ts, sdk/core/tests/cli.config.test.ts</files>
  <action>Implement strict runtime config parsing (default path + `--config` override), canonical env namespace (`SSS_TOKEN_*`), merge precedence (`flags > env > file`), and signer sourcing (global default plus per-command role overrides). Fail fast on invalid config and missing signer requirements with stable CLI error categories.</action>
  <verify>yarn test:sdk</verify>
  <done>Runtime input resolution is deterministic and contract-tested for CLI-03.</done>
</task>

<task type="auto">
  <name>Task 4: Implement output and exit-code contract</name>
  <files>sdk/core/src/cli/output.ts, sdk/core/src/cli/errors.ts, sdk/core/tests/cli.output.test.ts</files>
  <action>Add human-readable renderer plus stable JSON envelope (`ok`, `command`, `data`, `error`) and map SDK/local/usage failures to deterministic non-zero exits. Ensure SDK error codes are surfaced in CLI output when available.</action>
  <verify>yarn test:sdk</verify>
  <done>CLI success/error output is stable for operators and automation consumers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn workspace @stbr/sss-token build`
- [ ] `yarn test:sdk`
- [ ] CLI runtime precedence is proven as `flags > env > file`
- [ ] Failures exit non-zero and JSON output includes stable SDK error codes when present
- [ ] SDK init path is real (not simulated) for CLI init usage
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- CLI foundation and runtime-config contracts are complete for Phase 6 command implementation
</success_criteria>

<output>
After completion, create .planning/phases/06-admin-cli/06-01-SUMMARY.md
</output>
