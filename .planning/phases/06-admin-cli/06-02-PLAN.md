---
phase: 06-admin-cli
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - sdk/core/src/cli.ts
  - sdk/core/src/cli/commands/init.ts
  - sdk/core/src/cli/commands/lifecycle.ts
  - sdk/core/src/cli/commands/admin.ts
  - sdk/core/src/cli/commands/minters.ts
  - sdk/core/src/cli/parsers.ts
  - sdk/core/src/cli/confirm.ts
  - sdk/core/tests/cli.init.test.ts
  - sdk/core/tests/cli.lifecycle.test.ts
  - sdk/core/tests/cli.admin.test.ts
autonomous: true
requirements: [CLI-01, CLI-02]
user_setup: []
must_haves:
  truths:
    - Operators can initialize and manage SSS-1, SSS-2, and custom deployments through explicit CLI commands wired to SDK contracts.
    - Lifecycle/admin commands (`mint`, `burn`, `freeze`, `thaw`, `pause`, `unpause`, roles/authority/minter updates, `status`, `supply`) enforce required arguments and signer requirements before SDK calls.
    - High-impact mutations support explicit confirmation bypass (`--yes`) while preserving automation-safe deterministic behavior.
  artifacts:
    - sdk/core/src/cli/commands/init.ts
    - sdk/core/src/cli/commands/lifecycle.ts
    - sdk/core/src/cli/commands/admin.ts
    - sdk/core/src/cli/commands/minters.ts
    - sdk/core/src/cli/parsers.ts
    - sdk/core/tests/cli.init.test.ts
    - sdk/core/tests/cli.lifecycle.test.ts
    - sdk/core/tests/cli.admin.test.ts
  key_links:
    - Command contracts remain a thin orchestration layer over SDK methods and inherit SDK typed error behavior.
    - README command expectations are preserved where executable in current phase scope.
---

<objective>
Implement the Phase 6 init, lifecycle, and admin command surface for daily operator workflows.

Purpose: Satisfy CLI-01 and CLI-02 by wiring explicit command contracts to SDK create/load and lifecycle/admin APIs.
Output: Executable `sss-token` commands for initialization, lifecycle mutations, role updates, and supply/state queries.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-cli/06-CONTEXT.md
@.planning/phases/06-admin-cli/06-RESEARCH.md
@.planning/phases/06-admin-cli/06-VALIDATION.md
@CLAUDE.md
@README.md
@.planning/phases/06-admin-cli/06-01-SUMMARY.md
@sdk/core/src/stablecoin.ts
@sdk/core/src/config.ts
@sdk/core/src/errors.ts
@sdk/core/src/cli.ts
@sdk/core/src/cli/context.ts
@sdk/core/src/cli/output.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement `init` command for preset and custom deployment flows</name>
  <files>sdk/core/src/cli/commands/init.ts, sdk/core/src/cli/parsers.ts, sdk/core/tests/cli.init.test.ts</files>
  <action>Implement `sss-token init --preset sss-1|sss-2` and `sss-token init --custom <path>` with explicit required metadata/options. Route to SDK `create()` using resolved runtime signer/rpc settings and preserve Phase 4 config semantics for strict custom config validation.</action>
  <verify>yarn test:sdk</verify>
  <done>CLI init handles SSS-1, SSS-2, and custom flows with deterministic validation and output.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lifecycle mutation and query commands</name>
  <files>sdk/core/src/cli/commands/lifecycle.ts, sdk/core/src/cli/parsers.ts, sdk/core/tests/cli.lifecycle.test.ts</files>
  <action>Implement `mint`, `burn`, `freeze`, `thaw`, `pause`, `unpause`, `status`, and `supply` command handlers. Enforce positional argument parsing (`PublicKey`, `bigint`, mint account inputs), per-command signer requirements, and `--yes` confirmation control for destructive operations where needed.</action>
  <verify>yarn test:sdk</verify>
  <done>Lifecycle operator command surface is complete and consistent with SDK/on-chain contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Implement admin and minter-management command handlers</name>
  <files>sdk/core/src/cli/commands/admin.ts, sdk/core/src/cli/commands/minters.ts, sdk/core/tests/cli.admin.test.ts</files>
  <action>Implement role and admin workflows (`roles update`, `authority transfer`, `treasury set`, `minters add/remove/get`) with explicit signer routing and quota parsing. Treat `minters get` ownership as Plan 02 only to avoid cross-plan command-scope conflicts. Keep command names explicit and grouped per context decisions.</action>
  <verify>yarn test:sdk</verify>
  <done>Admin command set supports explicit operator role management and minter controls.</done>
</task>

<task type="auto">
  <name>Task 4: Wire command registration and process-level CLI contract tests</name>
  <files>sdk/core/src/cli.ts, sdk/core/tests/cli.init.test.ts, sdk/core/tests/cli.lifecycle.test.ts, sdk/core/tests/cli.admin.test.ts</files>
  <action>Register lifecycle/admin/init command trees in the root CLI program and add process-oriented tests validating stdout/stderr, JSON/human rendering, and exit codes for representative success/failure paths.</action>
  <verify>yarn workspace @stbr/sss-token build && yarn test:sdk</verify>
  <done>Core command contracts are executable via built CLI binary and validated end-to-end at process level.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn workspace @stbr/sss-token build`
- [ ] `yarn test:sdk`
- [ ] `init` supports SSS-1, SSS-2, and custom config modes
- [ ] Lifecycle + admin commands execute with deterministic parsing, signer enforcement, and non-zero failure exits
- [ ] README core command surface parity is maintained for implemented commands
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- CLI-01 and CLI-02 operator flows are executable and contract-tested
</success_criteria>

<output>
After completion, create .planning/phases/06-admin-cli/06-02-SUMMARY.md
</output>
