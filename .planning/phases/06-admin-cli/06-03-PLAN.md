---
phase: 06-admin-cli
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - sdk/core/src/cli/commands/compliance.ts
  - sdk/core/src/cli/commands/management.ts
  - sdk/core/src/cli/output.ts
  - sdk/core/src/cli/errors.ts
  - sdk/core/src/cli/context.ts
  - sdk/core/tests/cli.compliance.test.ts
  - sdk/core/tests/cli.management.test.ts
  - sdk/core/tests/cli.integration.test.ts
  - README.md
autonomous: true
requirements: [CLI-02, CLI-03]
user_setup: []
must_haves:
  truths:
    - SSS-2 compliance commands are fully available in CLI (`blacklist add/remove/check`, `seize`) with variant gating and stable error handling.
    - Runtime behavior is automation-safe: deterministic output schema, stable exit codes, and explicit unsupported-operation failures for incompatible variants.
    - Management command scope is explicit and executable for this phase (excluding `minters get`, owned by Plan 02), with non-implementable placeholders either concretely implemented or documented as deferred.
  artifacts:
    - sdk/core/src/cli/commands/compliance.ts
    - sdk/core/src/cli/commands/management.ts
    - sdk/core/src/cli/output.ts
    - sdk/core/src/cli/errors.ts
    - sdk/core/tests/cli.compliance.test.ts
    - sdk/core/tests/cli.management.test.ts
    - sdk/core/tests/cli.integration.test.ts
    - README.md
  key_links:
    - Compliance command handlers rely on SDK compliance module contracts and propagate machine-readable SDK error codes.
    - Final CLI command inventory and behavior are codified in tests and repository docs for downstream Phase 7/9 consumers.
---

<objective>
Complete SSS-2 compliance and management command coverage with final hardening for automation and documentation parity.

Purpose: Close CLI-02/CLI-03 by delivering compliance commands, management-path decisions, and requirement-mapped integration validation.
Output: Full compliance operator surface and finalized runtime behavior contracts ready for backend-service reuse.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-cli/06-CONTEXT.md
@.planning/phases/06-admin-cli/06-RESEARCH.md
@.planning/phases/06-admin-cli/06-VALIDATION.md
@CLAUDE.md
@README.md
@.planning/phases/06-admin-cli/06-01-SUMMARY.md
@sdk/core/src/compliance.ts
@sdk/core/src/errors.ts
@sdk/core/src/cli.ts
@sdk/core/src/cli/context.ts
@sdk/core/src/cli/output.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SSS-2 compliance command handlers</name>
  <files>sdk/core/src/cli/commands/compliance.ts, sdk/core/src/cli/context.ts, sdk/core/tests/cli.compliance.test.ts</files>
  <action>Implement `blacklist add`, `blacklist remove`, `blacklist check`, and `seize` commands with explicit required operands/flags and signer routes. Enforce SSS-2-only gating and propagate stable SDK error codes in both human and JSON output modes.</action>
  <verify>yarn test:sdk</verify>
  <done>Compliance commands are executable, deterministic, and compatible with automation flows.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve non-minter management scope into executable behavior</name>
  <files>sdk/core/src/cli/commands/management.ts, README.md, sdk/core/tests/cli.management.test.ts</files>
  <action>Implement realistic Phase 6 non-minter management commands from README surface (`holders`, `audit-log`) where direct SDK/program-account queries are feasible. For any command not feasible in phase scope, convert to explicit deferred behavior with deterministic unsupported messaging and update README contract accordingly. Do not re-own `minters get` in this plan.</action>
  <verify>yarn test:sdk</verify>
  <done>Management command inventory is explicit, executable, and no longer ambiguous for operators.</done>
</task>

<task type="auto">
  <name>Task 3: Harden CLI integration contracts and exit semantics</name>
  <files>sdk/core/src/cli/output.ts, sdk/core/src/cli/errors.ts, sdk/core/tests/cli.integration.test.ts</files>
  <action>Add cross-command integration tests covering process exit behavior, JSON schema stability, SDK error passthrough, variant incompatibility errors, and representative env/config/flag override paths spanning lifecycle and compliance commands.</action>
  <verify>yarn workspace @stbr/sss-token build && yarn test:sdk && yarn test:sss1 && yarn test:sss2</verify>
  <done>CLI behavior is stable for operator scripting and downstream automation consumers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn workspace @stbr/sss-token build`
- [ ] `yarn test:sdk`
- [ ] `yarn test:sss1 && yarn test:sss2`
- [ ] Compliance commands (`blacklist add/remove/check`, `seize`) are implemented and SSS-2 gated
- [ ] Management commands are either implemented with tests or explicitly deferred with deterministic behavior and docs updates
- [ ] JSON envelope and exit-code policy remain stable across representative command families
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- CLI-02 and CLI-03 are satisfied for compliance + runtime behavior contracts
</success_criteria>

<output>
After completion, create .planning/phases/06-admin-cli/06-03-SUMMARY.md
</output>
