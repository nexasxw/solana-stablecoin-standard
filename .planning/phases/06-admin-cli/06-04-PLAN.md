---
phase: 06-admin-cli
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
gap_closure: true
files_modified:
  - tests/sss-2.ts
  - tests/helpers/token2022.ts
  - .planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
user_setup: []
must_haves:
  truths:
    - The Phase 6 verification chain is green end-to-end, including `yarn test:sss2`, so CLI completion is proven without unresolved regressions.
    - The three failing SSS-2 test cases are repaired with deterministic setup and no mutable-const runtime failures.
    - Seizure precondition tests use valid Token-2022 mint/account setup so failures assert intended program errors (not setup artifacts like `Invalid Mint`).
  artifacts:
    - tests/sss-2.ts
    - tests/helpers/token2022.ts
    - .planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md
  key_links:
    - Fixing the failing SSS-2 suite is required by Plan 06-03 verification gates before Phase 6 can be marked complete.
    - This plan closes verification-only gaps and does not expand Phase 6 scope.
---

<objective>
Close unresolved Phase 6 verification gaps by repairing the failing `tests/sss-2.ts` cases and re-running the required verification command chain.

Purpose: Convert Phase 6 from "implemented but unverified" to "verified complete" by eliminating false-negative test failures.
Output: Green `yarn test:sss2` and updated verification evidence for 06-admin-cli.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-cli/06-03-PLAN.md
@.planning/phases/06-admin-cli/06-03-SUMMARY.md
@.planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md
@CLAUDE.md
@tests/sss-2.ts
@tests/helpers/token2022.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mutable-const runtime failures in SSS-2 tests</name>
  <files>tests/sss-2.ts</files>
  <action>Repair the two failing tests that currently throw `TypeError: Assignment to constant variable.` by removing illegal reassignment patterns while preserving intent and assertion coverage.</action>
  <verify>yarn test:sss2 -- --grep "supports compliance role rotation|mints to real Token-2022 accounts"</verify>
  <done>Both previously failing tests execute successfully and assert intended behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Invalid Mint setup path in seizure precondition test</name>
  <files>tests/sss-2.ts, tests/helpers/token2022.ts</files>
  <action>Correct the `requires treasury, freeze, and blacklist for seizure` setup so Token-2022 accounts are created against initialized mints in deterministic order. If helper assumptions are brittle, harden helper behavior to prevent pre-mint ATA creation misuse.</action>
  <verify>yarn test:sss2 -- --grep "requires treasury, freeze, and blacklist for seizure"</verify>
  <done>The test fails only for the targeted on-chain preconditions (`TreasuryNotConfigured`, `SeizeTargetNotFrozen`, `SeizeTargetNotBlacklisted`) and no longer fails with `Invalid Mint` setup errors.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run full Phase 6 verification chain and refresh evidence</name>
  <files>.planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md</files>
  <action>Execute the required command sequence from 06-03 verification gate (`yarn workspace @stbr/sss-token build && yarn test:sdk && yarn test:sss1 && yarn test:sss2`) and update verification notes with final pass/fail evidence and verdict.</action>
  <verify>yarn workspace @stbr/sss-token build && yarn test:sdk && yarn test:sss1 && yarn test:sss2</verify>
  <done>Verification report reflects successful completion with no remaining Phase 6 gaps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn test:sss2` passes without the three previously reported failures
- [ ] `yarn workspace @stbr/sss-token build`
- [ ] `yarn test:sdk`
- [ ] `yarn test:sss1`
- [ ] `yarn test:sss2`
- [ ] `.planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md` updated to `status: verified` (or equivalent no-gap verdict)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No unresolved verification gaps remain for Phase 6
</success_criteria>

<output>
After completion, create .planning/phases/06-admin-cli/06-04-SUMMARY.md
</output>
