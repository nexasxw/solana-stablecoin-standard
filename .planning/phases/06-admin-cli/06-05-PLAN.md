---
phase: 06-admin-cli
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
gap_closure: true
files_modified:
  - tests/sss-2.ts
  - tests/helpers/index.ts
  - .planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
user_setup: []
must_haves:
  truths:
    - `yarn test:sss2` is green with no constant-reassignment runtime failures in `tests/sss-2.ts`.
    - Seizure precondition setup builds valid Token-2022 mint/account state, so assertions exercise intended program errors (not setup artifacts like `Invalid Mint`).
    - Phase 6 verification evidence is refreshed after fixes, preserving requirement traceability across `CLI-01`, `CLI-02`, and `CLI-03`.
  artifacts:
    - tests/sss-2.ts
    - tests/helpers/index.ts
    - .planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md
  key_links:
    - This plan is an incremental gap-closure follow-up to `06-04-PLAN.md` and does not widen Phase 6 scope.
    - Passing `yarn test:sss2` is required before Phase 6 can move from `gaps_found` to verified completion.
---

<objective>
Close remaining Phase 6 verification gaps by fixing three failing `yarn test:sss2` cases and re-establishing end-to-end verification proof.

Purpose: Remove deterministic test regressions that currently block phase closeout.
Output: Green SSS-2 test suite and updated verification report with no unresolved Phase 6 gaps.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-cli/06-04-PLAN.md
@.planning/phases/06-admin-cli/06-04-SUMMARY.md
@.planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md
@tests/sss-2.ts
@tests/helpers/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair constant reassignment in compliance role rotation test</name>
  <files>tests/sss-2.ts</files>
  <action>Fix the `TypeError: Assignment to constant variable` failure at `tests/sss-2.ts:171` in `supports compliance role rotation while preserving authority override` by replacing illegal const reassignment with deterministic mutable state handling that preserves the original test intent and assertions.</action>
  <verify>yarn test:sss2 -- --grep "supports compliance role rotation while preserving authority override"</verify>
  <done>The role-rotation test passes and still validates authority-override behavior without runtime assignment errors.</done>
</task>

<task type="auto">
  <name>Task 2: Repair constant reassignment in Token-2022 minting test</name>
  <files>tests/sss-2.ts</files>
  <action>Fix the `TypeError: Assignment to constant variable` failure at `tests/sss-2.ts:225` in `mints to real Token-2022 accounts for later hook tests` while preserving intended mint/account flow coverage and deterministic fixture behavior.</action>
  <verify>yarn test:sss2 -- --grep "mints to real Token-2022 accounts for later hook tests"</verify>
  <done>The Token-2022 minting test passes and continues to cover real-account mint setup for downstream hook scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Fix seizure precondition setup Invalid Mint path</name>
  <files>tests/sss-2.ts, tests/helpers/index.ts</files>
  <action>Correct seizure precondition setup ordering/inputs so `createToken2022Ata` receives a valid initialized mint and owner context, eliminating `Invalid Mint` from `tests/helpers/index.ts:122`. Keep assertions focused on intended SSS-2 failures (`TreasuryNotConfigured`, `SeizeTargetNotFrozen`, `SeizeTargetNotBlacklisted`).</action>
  <verify>yarn test:sss2 -- --grep "requires treasury, freeze, and blacklist for seizure"</verify>
  <done>The seizure precondition test no longer fails during ATA setup and validates only expected program-level precondition errors.</done>
</task>

<task type="auto">
  <name>Task 4: Re-run verification chain and refresh phase verdict</name>
  <files>.planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md</files>
  <action>Run `yarn test:sss2` after targeted fixes, then run the full Phase 6 chain (`yarn workspace @stbr/sss-token build && yarn test:sdk && yarn test:sss1 && yarn test:sss2`) and update verification evidence/status accordingly.</action>
  <verify>yarn workspace @stbr/sss-token build && yarn test:sdk && yarn test:sss1 && yarn test:sss2</verify>
  <done>Verification report is updated with current evidence and reflects closure of this gap plan.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn test:sss2` passes with all 3 previously failing cases fixed
- [ ] `yarn workspace @stbr/sss-token build`
- [ ] `yarn test:sdk`
- [ ] `yarn test:sss1`
- [ ] `yarn test:sss2`
- [ ] `.planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md` updated with post-fix evidence and final verdict
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No unresolved Phase 6 gaps remain for CLI-01, CLI-02, CLI-03
</success_criteria>

<output>
After completion, create .planning/phases/06-admin-cli/06-05-SUMMARY.md
</output>
