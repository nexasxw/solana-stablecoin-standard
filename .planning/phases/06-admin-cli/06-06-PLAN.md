---
phase: 06-admin-cli
plan: 06
type: execute
wave: 6
depends_on: ["06-05"]
gap_closure: true
files_modified:
  - package.json
  - README.md
  - scripts/sss-token
  - scripts/install-sss-token.sh
  - sdk/core/tests/cli.integration.test.ts
  - .planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
user_setup: []
must_haves:
  truths:
    - Repo operators have a deterministic documented path to execute the CLI command contract from shell without relying on hidden environment assumptions.
    - CLI invocation guidance and examples in README match the actual runnable command path used by UAT.
    - Verification evidence includes a shell-level command availability check plus existing lifecycle/compliance JSON-envelope behavior checks.
  artifacts:
    - package.json
    - README.md
    - scripts/sss-token
    - scripts/install-sss-token.sh
    - .planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md
  key_links:
    - Diagnosed from `.planning/phases/06-admin-cli/06-UAT.md` tests 1-3 (`sss-token: command not found`).
    - This plan only closes command-exposure/documentation gaps and keeps Phase 6 scope unchanged.
---

<objective>
Close the remaining Phase 6 UAT gaps by making CLI invocation deterministic for operators and aligning documentation + verification with the runnable shell contract.

Purpose: Ensure UAT command checks exercise real CLI behavior instead of failing at shell command resolution.
Output: Reliable operator command path, updated docs, and refreshed Phase 6 verification evidence.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-admin-cli/06-UAT.md
@.planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md
@README.md
@package.json
@sdk/core/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship and document deterministic CLI invocation path</name>
  <files>scripts/sss-token, scripts/install-sss-token.sh, package.json, README.md</files>
  <action>Create a repo-level executable wrapper for the SDK CLI, add a helper installer script that links it into user-shell location (e.g. `~/.local/bin/sss-token`) with safe idempotent behavior, add root npm/yarn aliases, and update README examples to use the same invocation contract.</action>
  <verify>./scripts/sss-token --help && ./scripts/install-sss-token.sh --dry-run && rg -n \"sss-token|scripts/sss-token|install-sss-token\" README.md</verify>
  <done>Operators have one documented invocation path that is runnable from a clean local checkout.</done>
</task>

<task type="auto">
  <name>Task 2: Add fast regression check for invocation contract</name>
  <files>sdk/core/tests/cli.integration.test.ts</files>
  <action>Add integration coverage that validates the documented repo-level invocation path reaches the CLI parser and returns structured usage/runtime responses (not shell `command not found`). Ensure the task has a fast targeted verify command.</action>
  <verify>yarn test:sdk -- --grep \"invocation contract\"</verify>
  <done>Regression test protects against future breakage of the documented invocation path.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh phase verification evidence after command-path fix</name>
  <files>.planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md</files>
  <action>Re-run UAT command checks using the documented invocation path, then run full Phase 6 verification chain and update verification report status/evidence to reflect closure of tests 1-3 gaps.</action>
  <verify>./scripts/sss-token --help && rg -n \"UAT|command not found|Final Verdict|passed\" .planning/phases/06-admin-cli/06-admin-cli-VERIFICATION.md</verify>
  <done>Verification report includes shell-level command availability proof and no unresolved Phase 6 CLI execution-path gaps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Repo-level CLI command path is documented and runnable in a fresh checkout
- [ ] README command examples use the same documented path
- [ ] SDK integration test covers invocation contract and passes
- [ ] `yarn workspace @stbr/sss-token build`
- [ ] `yarn test:sdk`
- [ ] `yarn test:sss1`
- [ ] `yarn test:sss2`
- [ ] Phase 6 verification report updated with post-fix command-path evidence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- UAT tests 1-3 no longer fail due to shell `command not found`
</success_criteria>

<output>
After completion, create .planning/phases/06-admin-cli/06-06-SUMMARY.md
</output>
