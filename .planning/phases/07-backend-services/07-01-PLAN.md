---
plan: 01
type: execute
phase: 07-backend-services
title: Shared Service Contracts And Persistence Foundation
wave: 1
depends_on: []
requirements: [SRV-01, SRV-02, SRV-03, SRV-04]
files_modified:
  - package.json
  - services/shared/package.json
  - services/shared/tsconfig.json
  - services/shared/src/contracts/envelope.ts
  - services/shared/src/contracts/errors.ts
  - services/shared/src/contracts/jobs.ts
  - services/shared/src/db/schema.sql
  - services/shared/src/index.ts
autonomous: true
must_haves:
  truths:
    - All service APIs return one canonical envelope (`success`, `data`, `error`, `code`, `request_id`, `timestamp`) and keep machine-readable `code` stable.
    - Shared async job lifecycle states are fixed to `queued|running|succeeded|failed|canceled`.
    - Shared persistence primitives for idempotency and service jobs are defined once and reused by all services.
    - Event envelopes remain versioned and include explicit `event_version` for downstream compatibility.
  artifacts:
    - services/shared/src/contracts/envelope.ts
    - services/shared/src/contracts/errors.ts
    - services/shared/src/contracts/jobs.ts
    - services/shared/src/db/schema.sql
    - services/shared/src/index.ts
  key_links:
    - Shared envelope/error/job contracts are consumed unchanged by mint-burn, indexer, compliance, and webhook plans.
    - Shared schema primitives are extended in later plans but baseline invariants are locked here.
---

<objective>
Establish the shared backend contracts and persistence baseline used by every Phase 7 service.

Purpose: lock cross-service response/error/job contracts before service-specific execution work begins.
Output: reusable `services/shared` package and baseline schema primitives.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-backend-services/07-CONTEXT.md
@.planning/phases/07-backend-services/07-RESEARCH.md
@.planning/phases/07-backend-services/07-VALIDATION.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared service workspace and canonical contracts</name>
  <files>package.json, services/shared/package.json, services/shared/tsconfig.json, services/shared/src/contracts/envelope.ts, services/shared/src/contracts/errors.ts, services/shared/src/contracts/jobs.ts, services/shared/src/index.ts</files>
  <action>Ownership: shared package and contract files only. Add `services/shared` workspace with canonical response envelope helpers, stable error-code taxonomy mapper, and shared job lifecycle/event envelope types.</action>
  <verify>yarn build</verify>
  <done>All services can import one canonical envelope/error/job contract from `services/shared`.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared schema baseline for service jobs and idempotency</name>
  <files>services/shared/src/db/schema.sql</files>
  <action>Ownership: shared schema baseline only. Add foundational tables and constraints for service jobs and idempotency persistence (`tenant_id + idempotency_key + fingerprint + first_response`) without service-specific columns that belong to later plans.</action>
  <verify>yarn build</verify>
  <done>Durable cross-service persistence invariants are locked before service API implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Prove cross-service contract imports compile cleanly</name>
  <files>services/shared/src/index.ts</files>
  <action>Ownership: shared package export surface only. Finalize stable exports used by mint-burn, indexer, compliance, and webhook so later plans consume one source of truth without local envelope/error drift.</action>
  <verify>yarn build</verify>
  <done>Shared contracts are consumable by all downstream Phase 7 plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn lint`
- [ ] `yarn build`
- [ ] Canonical envelope fields are exported and documented in shared contracts
- [ ] Shared job lifecycle states remain `queued|running|succeeded|failed|canceled`
- [ ] Shared schema includes durable idempotency and job primitives with deterministic uniqueness constraints
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Shared contracts/persistence foundation needed by SRV-01..04 is locked
</success_criteria>

<output>
After completion, create .planning/phases/07-backend-services/07-01-SUMMARY.md
</output>
