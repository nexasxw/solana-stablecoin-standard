---
plan: 04
type: execute
phase: 07-backend-services
title: Issuance API, Idempotency, And Worker Execution
wave: 2
depends_on: [07-01]
requirements: [SRV-01]
files_modified:
  - services/shared/src/db/schema.sql
  - services/shared/src/middleware/request-context.ts
  - services/shared/src/auth/service-auth.ts
  - services/shared/src/middleware/idempotency.ts
  - services/mint-burn/src/routes/issuance.ts
  - services/mint-burn/src/jobs/issuance-worker.ts
  - services/mint-burn/src/store/issuance-repository.ts
  - services/mint-burn/src/__tests__/issuance.api.test.ts
  - services/mint-burn/src/__tests__/issuance.worker.test.ts
autonomous: true
must_haves:
  truths:
    - All mutating issuance requests enforce durable idempotency with deterministic replay and deterministic conflict on payload mismatch.
    - Issuance exposes separate async mint and burn job contracts with lifecycle states `queued|running|succeeded|failed|canceled`.
    - Request identity chain (requester, approver, executing service identity) is persisted on mutating job records.
    - Issuer-only authorization is enforced and operator override/approval carries verifiable intent-signature context persisted with the job identity chain.
  artifacts:
    - services/shared/src/middleware/idempotency.ts
    - services/shared/src/auth/service-auth.ts
    - services/mint-burn/src/routes/issuance.ts
    - services/mint-burn/src/jobs/issuance-worker.ts
    - services/mint-burn/src/__tests__/issuance.api.test.ts
    - services/mint-burn/src/__tests__/issuance.worker.test.ts
  key_links:
    - Issuance job records become normalized internal event sources consumed by indexer projection and webhook fanout plans.
---

<objective>
Deliver SRV-01 issuance command path with shared auth/context/idempotency middleware and deterministic worker execution.

Purpose: make mint/burn backend mutation flows production-safe and replay-safe.
Output: issuance APIs, async workers, and deterministic SRV-01 regression tests.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-backend-services/07-CONTEXT.md
@.planning/phases/07-backend-services/07-RESEARCH.md
@.planning/phases/07-backend-services/07-VALIDATION.md
@services/mint-burn/src/index.ts
@sdk/core/src/stablecoin.ts
@sdk/core/src/errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement request context, service auth, and durable idempotency middleware</name>
  <files>services/shared/src/middleware/request-context.ts, services/shared/src/auth/service-auth.ts, services/shared/src/middleware/idempotency.ts</files>
  <action>Ownership: shared middleware/auth files only. Implement request-id propagation and tenant scoping middleware, issuer-only service auth primitives, and idempotency persistence behavior with deterministic replay/conflict semantics.</action>
  <verify>yarn workspace @stbr/sss-mint-burn test</verify>
  <done>Mutating request invariants are enforced before issuance job admission.</done>
</task>

<task type="auto">
  <name>Task 2: Build separate mint and burn job APIs with persisted identity chain</name>
  <files>services/shared/src/db/schema.sql, services/mint-burn/src/routes/issuance.ts, services/mint-burn/src/store/issuance-repository.ts</files>
  <action>Ownership: mint-burn issuance API/repository plus issuance-specific schema migrations. Implement persistence for issuance job records and identity chain fields; add `POST /v1/issuance/mint-jobs`, `POST /v1/issuance/burn-jobs`, `GET /v1/issuance/jobs/:jobId`, and tenant-scoped list queries with canonical envelope responses.</action>
  <verify>yarn workspace @stbr/sss-mint-burn test</verify>
  <done>SRV-01 async issuance contracts are stable and deterministic for callers.</done>
</task>

<task type="auto">
  <name>Task 3: Implement issuance worker orchestration and SRV-01 regression coverage</name>
  <files>services/mint-burn/src/jobs/issuance-worker.ts, services/mint-burn/src/__tests__/issuance.api.test.ts, services/mint-burn/src/__tests__/issuance.worker.test.ts</files>
  <action>Ownership: mint-burn worker/tests only. Execute queued jobs through `@stbr/sss-token` SDK (no chain logic duplication), track lifecycle transitions, persist transaction metadata, emit normalized internal events, and add deterministic regression tests for idempotency replay/conflict and status transitions.</action>
  <verify>yarn workspace @stbr/sss-mint-burn test</verify>
  <done>Mint/burn jobs reach deterministic terminal outcomes with objective SRV-01 evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn lint`
- [ ] `yarn build`
- [ ] `yarn workspace @stbr/sss-mint-burn test`
- [ ] Mint and burn remain separate async job APIs with deterministic retrieval/listing
- [ ] Idempotency replay returns prior response; payload mismatch returns deterministic conflict code
- [ ] Issuer-only authorization and intent-signature context are persisted and validated in tests
- [ ] Issuance internal events include `event_id,event_type,event_version,request_id,occurred_at`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- SRV-01 is satisfied with deterministic API + worker + test evidence
</success_criteria>

<output>
After completion, create .planning/phases/07-backend-services/07-04-SUMMARY.md
</output>
