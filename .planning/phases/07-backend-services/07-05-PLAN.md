---
plan: 05
type: execute
phase: 07-backend-services
title: Webhook Subscriptions, Delivery Guarantees, And Signature Rotation
wave: 3
depends_on: [07-02, 07-04]
requirements: [SRV-04]
files_modified:
  - services/shared/src/db/schema.sql
  - services/webhook/src/routes/subscriptions.ts
  - services/webhook/src/routes/deliveries.ts
  - services/webhook/src/jobs/delivery-worker.ts
  - services/webhook/src/security/signature.ts
  - services/webhook/src/store/webhook-repository.ts
  - services/webhook/src/__tests__/delivery-ordering.test.ts
  - services/webhook/src/__tests__/retry-dlq.test.ts
  - services/webhook/src/__tests__/signature-rotation.test.ts
autonomous: true
must_haves:
  truths:
    - Webhook fanout transport is locked to a Postgres outbox table drained by Redis-backed delivery workers (no direct queue pub/sub fanout path).
    - Webhooks are at-least-once, ordered per entity key, signed with timestamped HMAC, retried with bounded exponential backoff, and dead-lettered on terminal failure.
    - Secret rotation supports bounded dual-key grace verification without delivery interruption.
    - Retention windows are contractual: webhook deliveries retained 90 days and webhook dead letters retained 180 days with deterministic purge behavior.
  artifacts:
    - services/webhook/src/routes/subscriptions.ts
    - services/webhook/src/routes/deliveries.ts
    - services/webhook/src/jobs/delivery-worker.ts
    - services/webhook/src/security/signature.ts
    - services/webhook/src/__tests__/delivery-ordering.test.ts
    - services/webhook/src/__tests__/signature-rotation.test.ts
  key_links:
    - Webhook fanout consumes indexer-normalized internal events and preserves request traceability from command origin.
---

<objective>
Deliver SRV-04 webhook subscription and delivery engine with deterministic retries, ordering, authenticity, and dead-letter semantics.

Purpose: provide downstream integration hooks with contractual reliability and signature verification behavior.
Output: webhook CRUD + delivery workers + regression suites for ordering/retry/signature-rotation invariants.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-backend-services/07-CONTEXT.md
@.planning/phases/07-backend-services/07-RESEARCH.md
@.planning/phases/07-backend-services/07-VALIDATION.md
@services/webhook/src/index.ts
@services/indexer/src/ingest/event-normalizer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement tenant-scoped webhook subscriptions and delivery observability routes</name>
  <files>services/shared/src/db/schema.sql, services/webhook/src/routes/subscriptions.ts, services/webhook/src/routes/deliveries.ts, services/webhook/src/store/webhook-repository.ts</files>
  <action>Ownership: webhook route/repository files plus webhook-specific schema migrations. Implement persistence for subscriptions, delivery attempts, and dead letters; add subscription CRUD with event filters/status controls and delivery history/read endpoints that expose deterministic attempt status and terminal DLQ visibility.</action>
  <verify>yarn workspace @stbr/sss-webhook test</verify>
  <done>Webhook consumers can manage subscriptions and inspect delivery state deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ordered delivery worker, retries, DLQ, and signature rotation</name>
  <files>services/shared/src/db/schema.sql, services/webhook/src/jobs/delivery-worker.ts, services/webhook/src/security/signature.ts, services/webhook/src/store/webhook-repository.ts</files>
  <action>Ownership: webhook worker/signature/repository plus delivery-retention schema migrations. Enforce per-entity ordering, at-least-once semantics, bounded exponential retry, terminal DLQ behavior, HMAC+timestamp signatures, dual-key grace window verification for secret rotation, and deterministic retention purge hooks.</action>
  <verify>yarn workspace @stbr/sss-webhook test</verify>
  <done>SRV-04 delivery and authenticity guarantees are enforced in implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Add SRV-04 regression tests for ordering, retry/DLQ, and rotation contracts</name>
  <files>services/webhook/src/__tests__/delivery-ordering.test.ts, services/webhook/src/__tests__/retry-dlq.test.ts, services/webhook/src/__tests__/signature-rotation.test.ts</files>
  <action>Ownership: webhook tests only. Add deterministic tests proving per-entity ordered delivery, timeout/non-2xx retry to DLQ, and dual-key signature verification during key rotation windows.</action>
  <verify>yarn workspace @stbr/sss-webhook test</verify>
  <done>SRV-04 is proven by objective deterministic regression evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn lint`
- [ ] `yarn build`
- [ ] `yarn workspace @stbr/sss-webhook test`
- [ ] Webhook delivery is at-least-once and ordered per entity key with bounded retry and terminal DLQ
- [ ] HMAC signature verification supports timestamp validation and dual-key rotation grace window
- [ ] Delivery/DLQ retention windows and purge behavior are explicit and deterministic
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- SRV-04 is satisfied with deterministic delivery and authenticity evidence
</success_criteria>

<output>
After completion, create .planning/phases/07-backend-services/07-05-SUMMARY.md
</output>
