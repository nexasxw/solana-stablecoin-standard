---
plan: 06
type: execute
phase: 07-backend-services
title: Cross-Service E2E Trace And Final Hardening
wave: 4
depends_on: [07-03, 07-05]
requirements: [SRV-01, SRV-02, SRV-03, SRV-04]
files_modified:
  - tests/integration.ts
  - services/compliance/src/__tests__/screening.test.ts
  - services/compliance/src/__tests__/audit-export.test.ts
  - services/webhook/src/index.ts
  - services/shared/src/db/schema.sql
autonomous: true
must_haves:
  truths:
    - End-to-end request trace continuity (`request_id`) from command request through projection and webhook attempt evidence is required for Phase 7 signoff.
    - Cross-service regressions must prove locked envelope/job/error contracts remain consistent at integration boundaries.
    - Retention purge behavior used by compliance exports and webhook evidence remains deterministic under integration execution.
  artifacts:
    - tests/integration.ts
    - services/compliance/src/__tests__/screening.test.ts
    - services/compliance/src/__tests__/audit-export.test.ts
  key_links:
    - Phase 7 signoff is blocked until one authoritative backend E2E path is deterministic and reproducible.
---

<objective>
Finalize Phase 7 with cross-service E2E evidence and integration-hardening regression tests.

Purpose: prove SRV-01..04 work together as one deterministic backend workflow.
Output: authoritative integration trace plus targeted hardening tests for boundary contracts.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-backend-services/07-CONTEXT.md
@.planning/phases/07-backend-services/07-RESEARCH.md
@.planning/phases/07-backend-services/07-VALIDATION.md
@tests/integration.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-service hardening tests for compliance/webhook contract boundaries</name>
  <files>services/compliance/src/__tests__/screening.test.ts, services/compliance/src/__tests__/audit-export.test.ts, services/webhook/src/index.ts, services/shared/src/db/schema.sql</files>
  <action>Ownership: compliance hardening tests, webhook boot wiring, and shared retention schema hooks only. Add focused regression coverage and minimal service wiring updates that prove boundary behavior stays deterministic for screening reason codes, audit export lifecycle, and retention/purge semantics.</action>
  <verify>yarn workspace @stbr/sss-compliance test && yarn workspace @stbr/sss-webhook test</verify>
  <done>Boundary contracts remain stable under cross-service regression pressure.</done>
</task>

<task type="auto">
  <name>Task 2: Implement authoritative backend E2E trace with request_id continuity evidence</name>
  <files>tests/integration.ts</files>
  <action>Ownership: root integration test path only. Add one authoritative E2E trace from issuance/compliance action to indexer projection update to webhook delivery attempt, asserting stable envelope/error contracts and continuous `request_id` linkage across service boundaries.</action>
  <verify>yarn test:integration</verify>
  <done>Phase 7 has one deterministic end-to-end proof spanning SRV-01..04.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn lint`
- [ ] `yarn build`
- [ ] `yarn workspace @stbr/sss-compliance test`
- [ ] `yarn workspace @stbr/sss-webhook test`
- [ ] `yarn test:integration`
- [ ] One E2E trace proves request_id continuity from service command to webhook delivery evidence
- [ ] Cross-service envelope/job/error contracts remain deterministic in integration assertions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 7 has complete deterministic E2E signoff evidence across SRV-01..04
</success_criteria>

<output>
After completion, create .planning/phases/07-backend-services/07-06-SUMMARY.md
</output>
