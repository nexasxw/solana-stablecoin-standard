---
plan: 01
type: execute
phase: 08-testing-and-fuzzing
title: Validation Baseline And Command Truth
status: planned
wave: 0
depends_on: []
requirements: [TST-01, TST-03]
files_modified:
  - package.json
  - services/indexer/package.json
  - services/webhook/package.json
  - .planning/phases/08-testing-and-fuzzing/08-VALIDATION.md
  - docs/testing/phase-08-command-truth.md
autonomous: true
must_haves:
  truths:
    - Phase 8 verification commands must execute real tests; placeholder success scripts are not allowed.
    - Quick gate and full gate commands are locked in one authoritative validation contract before deeper test/fuzz work.
    - Workspace test scripts for indexer and webhook execute their real mocha suites under `src/__tests__`.
    - Devnet/stress lane command scaffolding is explicitly defined so TST-03 evidence can run deterministically later.
  artifacts:
    - package.json
    - services/indexer/package.json
    - services/webhook/package.json
    - .planning/phases/08-testing-and-fuzzing/08-VALIDATION.md
    - docs/testing/phase-08-command-truth.md
  key_links:
    - package.json + services/indexer/package.json + services/webhook/package.json -> Phase 8 task verification commands -> via executable script contract in .planning/phases/08-testing-and-fuzzing/08-VALIDATION.md
    - 08-01 command-truth artifacts -> 08-02/08-03/08-04/08-05 plan verification gates -> via docs/testing/phase-08-command-truth.md and .planning/phases/08-testing-and-fuzzing/08-VALIDATION.md
---

<objective>
Lock Phase 8 verification command truth and remove automation drift before expanding coverage and fuzzing.

Purpose: make every subsequent plan verify against real, deterministic test execution.
Output: normalized workspace test scripts and refreshed validation contract for Phase 8.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-testing-and-fuzzing/08-RESEARCH.md
@.planning/phases/08-testing-and-fuzzing/08-VALIDATION.md
@package.json
@services/indexer/package.json
@services/webhook/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize root and workspace test scripts to real execution paths</name>
  <files>package.json, services/indexer/package.json, services/webhook/package.json</files>
  <action>Ownership: package script definitions only. Replace placeholder/no-op test script behavior with deterministic commands that execute real suites for indexer and webhook and keep root command aliases stable for Phase 8 gates.</action>
  <verify><automated>yarn workspace @stbr/sss-indexer run mocha -r ts-node/register --timeout 10000 --exit 'src/__tests__/**/*.test.ts' && yarn workspace @stbr/sss-webhook run mocha -r ts-node/register --timeout 10000 --exit 'src/__tests__/**/*.test.ts'</automated></verify>
  <done>Phase 8 validation commands call executable suites rather than placeholder success paths.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh phase validation strategy with quick/full/devnet lanes</name>
  <files>.planning/phases/08-testing-and-fuzzing/08-VALIDATION.md, docs/testing/phase-08-command-truth.md</files>
  <action>Ownership: validation and testing docs only. Update Nyquist task map, quick/full gate command set, and devnet proof lane prerequisites so downstream plans inherit one authoritative verification contract.</action>
  <verify><automated>rg -n "nyquist_compliant: true|Quick run command|Full suite command|Per-Task Verification Map" .planning/phases/08-testing-and-fuzzing/08-VALIDATION.md</automated></verify>
  <done>Validation contract and command truth are explicit, versioned, and reusable by all Phase 8 plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yarn lint`
- [ ] `yarn build`
- [ ] `yarn test:sss1`
- [ ] `yarn test:sss2`
- [ ] `yarn test:integration`
- [ ] `yarn test:sdk`
- [ ] `yarn workspace @stbr/sss-indexer run mocha -r ts-node/register --timeout 10000 --exit 'src/__tests__/**/*.test.ts'`
- [ ] `yarn workspace @stbr/sss-webhook run mocha -r ts-node/register --timeout 10000 --exit 'src/__tests__/**/*.test.ts'`
- [ ] `cargo check --manifest-path trident-tests/Cargo.toml -p fuzz_0`
- [ ] Validation doc reflects executable command truth with no placeholder lanes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 8 has one authoritative executable command contract for quick/full/devnet lanes
</success_criteria>

<output>
After completion, create .planning/phases/08-testing-and-fuzzing/08-01-SUMMARY.md
</output>
