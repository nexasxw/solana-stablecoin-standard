---
plan: 06
type: execute
phase: 08-testing-and-fuzzing
title: 08-03 Artifact Path Drift Closure
status: planned
wave: 4
depends_on: [08-03]
requirements: [TST-01]
gap_closure: true
files_modified:
  - .planning/phases/08-testing-and-fuzzing/08-03-PLAN.md
  - .planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md
autonomous: true
must_haves:
  truths:
    - Plan 08-03 file references, task ownership lists, and must-have artifact paths exactly match implemented regression files so strict artifact checks pass.
    - TST-01 evidence mapping remains intact after path correction and still points to executable regression commands.
  artifacts:
    - .planning/phases/08-testing-and-fuzzing/08-03-PLAN.md
    - .planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md
  key_links:
    - 08-03 regression matrix artifact contract -> TST-01 strict must-have conformance -> via corrected implemented SDK/service test paths.
---

<objective>
Close the Phase 08 verification gap caused by 08-03 planned artifact-path drift.

Purpose: restore strict plan-to-implementation conformance without changing shipped behavior or expanding test scope.
Output: corrected 08-03 plan artifact contract and refreshed 08 verification verdict with no remaining path-drift mismatch.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-testing-and-fuzzing/08-RESEARCH.md
@.planning/phases/08-testing-and-fuzzing/08-03-PLAN.md
@.planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md
@docs/testing/phase-08-regression-matrix.md
@sdk/core/tests/stablecoin.create.test.ts
@sdk/core/tests/stablecoin.lifecycle.test.ts
@services/mint-burn/src/__tests__/issuance.api.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct 08-03 planned artifact paths to implemented files</name>
  <files>.planning/phases/08-testing-and-fuzzing/08-03-PLAN.md</files>
  <action>Ownership: 08-03 plan document only. Update any stale path references in frontmatter, context, and task file lists so the artifact contract matches implemented regression files (`stablecoin.create.test.ts`, `stablecoin.lifecycle.test.ts`, `issuance.api.test.ts`) with no placeholder compatibility files added.</action>
  <verify><automated>bash -lc '! rg -n "stablecoin\.test\.ts|src/__tests__/api\.test\.ts" .planning/phases/08-testing-and-fuzzing/08-03-PLAN.md && rg -n "stablecoin\.create\.test\.ts|stablecoin\.lifecycle\.test\.ts|issuance\.api\.test\.ts" .planning/phases/08-testing-and-fuzzing/08-03-PLAN.md && test -f sdk/core/tests/stablecoin.create.test.ts && test -f sdk/core/tests/stablecoin.lifecycle.test.ts && test -f services/mint-burn/src/__tests__/issuance.api.test.ts'</automated></verify>
  <done>08-03 path contract is strictly aligned with actual repository artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh phase-08 verification report to close the drift finding</name>
  <files>.planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md</files>
  <action>Ownership: phase verification report only. Replace the 08-03 path-drift gap entry with a resolved note. Do not update final 08-VERIFICATION status/score until re-validation evidence is recorded from concrete artifact checks (`test -f` for all three implemented files, captured in the report with command/result lines and date), and status is transitioned off `gaps_found`.</action>
  <verify><automated>bash -lc 'test -f sdk/core/tests/stablecoin.create.test.ts && test -f sdk/core/tests/stablecoin.lifecycle.test.ts && test -f services/mint-burn/src/__tests__/issuance.api.test.ts && rg -n "Re-validation Evidence \(08-06\)" .planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md && rg -n "test -f sdk/core/tests/stablecoin\.create\.test\.ts|test -f sdk/core/tests/stablecoin\.lifecycle\.test\.ts|test -f services/mint-burn/src/__tests__/issuance\.api\.test\.ts" .planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md && rg -n "### Plan 08-03|Verdict: pass" .planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md && ! rg -n "^status:\\s*gaps_found$|stablecoin\\.test\\.ts|src/__tests__/api\\.test\\.ts" .planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md'</automated></verify>
  <done>Verification report reflects that strict artifact conformance is restored and phase status is no longer blocked by path drift.</done>
</task>

<task type="auto">
  <name>Task 3: Run documentation-contract sanity checks for requirement continuity</name>
  <files>.planning/phases/08-testing-and-fuzzing/08-03-PLAN.md, .planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md</files>
  <action>Ownership: listed planning docs only. Confirm TST-01 remains explicitly mapped to corrected artifacts in both 08-03 planning and 08 phase verification outputs.</action>
  <verify><automated>bash -lc 'test -f sdk/core/tests/stablecoin.create.test.ts && test -f sdk/core/tests/stablecoin.lifecycle.test.ts && test -f services/mint-burn/src/__tests__/issuance.api.test.ts && rg -n "TST-01" .planning/phases/08-testing-and-fuzzing/08-03-PLAN.md .planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md'</automated></verify>
  <done>TST-01 requirement traceability remains complete after the targeted 08-03 artifact mismatch closure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `! rg -n "stablecoin\.test\.ts|src/__tests__/api\.test\.ts" .planning/phases/08-testing-and-fuzzing/08-03-PLAN.md` returns no stale-path hits
- [ ] `rg -n "stablecoin\.create\.test\.ts|stablecoin\.lifecycle\.test\.ts|issuance\.api\.test\.ts" .planning/phases/08-testing-and-fuzzing/08-03-PLAN.md` confirms corrected-path presence
- [ ] `test -f sdk/core/tests/stablecoin.create.test.ts` passes
- [ ] `test -f sdk/core/tests/stablecoin.lifecycle.test.ts` passes
- [ ] `test -f services/mint-burn/src/__tests__/issuance.api.test.ts` passes
- [ ] `.planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md` no longer reports an open 08-03 artifact-path gap
- [ ] `.planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md` contains a `Re-validation Evidence (08-06)` section with dated command/result lines for the three `test -f` checks
- [ ] `! rg -n "stablecoin\.test\.ts|src/__tests__/api\.test\.ts" .planning/phases/08-testing-and-fuzzing/08-VERIFICATION.md` confirms stale gap strings are removed
- [ ] Verification status/score updates are applied only after the re-validation evidence block is present
- [ ] TST-01 continuity is explicit in both updated docs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 08 has no remaining verification gap tied to 08-03 artifact-path drift
</success_criteria>

<output>
After completion, create .planning/phases/08-testing-and-fuzzing/08-06-SUMMARY.md
</output>
