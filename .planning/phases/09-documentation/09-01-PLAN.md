---
plan: 01
type: execute
phase: 09-documentation
title: Documentation Inventory And Link Integrity Baseline
status: planned
wave: 0
depends_on: []
requirements: [DOC-01, DOC-02, DOC-03]
files_modified:
  - README.md
  - docs/ARCHITECTURE.md
  - docs/SSS-1.md
  - docs/SSS-2.md
  - docs/SDK.md
  - docs/COMPLIANCE.md
  - docs/API.md
  - docs/OPERATIONS.md
autonomous: true
must_haves:
  truths:
    - Phase 9 documentation work starts by locking a valid docs inventory and removing dead reviewer paths.
    - README documentation index must only point to files that exist after this plan.
    - Phase scope is documentation-only; no protocol, SDK, CLI, or service capability changes are allowed.
  artifacts:
    - README.md
    - docs/SDK.md
    - docs/COMPLIANCE.md
    - docs/API.md
    - docs/OPERATIONS.md
  key_links:
    - README docs index -> concrete docs/*.md files -> reviewer navigation contract for DOC-01
    - docs inventory baseline -> all downstream Phase 9 plans -> executable consistency gate
---

<objective>
Establish the Phase 9 documentation inventory and remove broken documentation paths before deeper content alignment.

Purpose: create one authoritative, valid doc index for reviewer navigation and downstream documentation execution.
Output: corrected README doc index and created/confirmed core documentation targets required by Phase 9.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-documentation/09-CONTEXT.md
@.planning/phases/09-documentation/09-RESEARCH.md
@.planning/phases/09-documentation/09-VALIDATION.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock canonical Phase 9 documentation inventory and README index paths</name>
  <files>README.md</files>
  <action>Ownership: README docs index sections only. Update or normalize documentation links so every reviewer path resolves to an existing file in the Phase 9 inventory.</action>
  <verify><automated>for f in $(grep -oE 'docs/[A-Za-z0-9_./-]+\\.md' README.md | sort -u); do test -f "$f" || { echo "missing:$f"; exit 1; }; done</automated></verify>
  <done>README documentation index contains no dead links and represents the canonical Phase 9 doc entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Create missing reviewer-facing doc files with placeholder-safe scaffolding</name>
  <files>docs/SDK.md, docs/COMPLIANCE.md, docs/API.md, docs/OPERATIONS.md</files>
  <action>Ownership: create missing docs only. Add minimal executable section scaffolds (prerequisites, command surfaces, verification references) so downstream plans can fill accurate content without changing product behavior.</action>
  <verify><automated>test -f docs/SDK.md && test -f docs/COMPLIANCE.md && test -f docs/API.md && test -f docs/OPERATIONS.md</automated></verify>
  <done>All Phase 9 target docs exist and are ready for requirement-linked content updates.</done>
</task>

<task type="auto">
  <name>Task 3: Add inventory and scope guard notes for documentation-only phase execution</name>
  <files>README.md, docs/OPERATIONS.md</files>
  <action>Ownership: docs narrative notes only. Add explicit statement that current docs describe shipped behavior and defer future-phase capabilities to avoid scope creep.</action>
  <verify><automated>rg -n "shipped behavior|documentation-only|Phase 9" README.md docs/OPERATIONS.md</automated></verify>
  <done>Reviewer docs clearly separate shipped behavior from deferred phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `for f in $(grep -oE 'docs/[A-Za-z0-9_./-]+\.md' README.md | sort -u); do test -f "$f" || { echo "missing:$f"; exit 1; }; done`
- [ ] `test -f docs/SDK.md && test -f docs/COMPLIANCE.md && test -f docs/API.md && test -f docs/OPERATIONS.md`
- [ ] `rg -n "DOC-01|DOC-02|DOC-03" .planning/phases/09-documentation/09-*.md`
- [ ] README and docs index path contract is deterministic and link-clean
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 9 documentation inventory is stable, complete, and executable as a baseline for downstream plans
</success_criteria>

<output>
After completion, create .planning/phases/09-documentation/09-01-SUMMARY.md
</output>
