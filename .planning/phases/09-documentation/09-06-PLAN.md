---
plan: 06
type: execute
phase: 09-documentation
title: DOC-02 Example Drift Closure And Verification Refresh
status: planned
wave: 4
depends_on: [09-05]
requirements: [DOC-01, DOC-02, DOC-03]
gap_closure: true
files_modified:
  - README.md
  - docs/SDK.md
  - docs/TRACEABILITY.md
  - .planning/phases/09-documentation/09-VERIFICATION.md
autonomous: true
must_haves:
  truths:
    - Documentation examples shown to reviewers must match current SDK and CLI interfaces exactly, including required parameter names and signer expectations.
    - Phase 9 remains documentation-only; this plan cannot change protocol, SDK runtime behavior, CLI implementation, or service code.
    - Gap closure is complete only when verification evidence records command-surface checks and the prior DOC-02 failures are explicitly resolved.
  artifacts:
    - README.md
    - docs/SDK.md
    - docs/TRACEABILITY.md
    - .planning/phases/09-documentation/09-VERIFICATION.md
  key_links:
    - DOC-02 parity closure -> README/SDK examples -> sdk/core source signatures and scripts/sss-token help surfaces
    - DOC-01 and DOC-03 continuity -> traceability and verification report updates -> reviewer-ready phase signoff inputs
---

<objective>
Close the remaining Phase 9 documentation gap by correcting SDK/CLI example drift and refreshing verification evidence.

Purpose: restore exact interface parity between docs and shipped command/API surfaces without changing implementation behavior.
Output: corrected examples, updated requirement traceability, and verification report moved from gaps_found to resolved.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-documentation/09-VERIFICATION.md
@README.md
@docs/SDK.md
@docs/TRACEABILITY.md
@sdk/core/src/stablecoin.ts
@sdk/core/src/compliance.ts
@sdk/core/src/cli/commands/lifecycle.ts
@sdk/core/src/cli/commands/compliance.ts
@scripts/sss-token
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct README SDK and CLI examples to match current interfaces</name>
  <files>README.md</files>
  <action>Ownership: README examples only. Fix documented SDK lifecycle/compliance snippets and CLI command examples so required arguments and flags match shipped signatures (`recipientTokenAccount`, bigint amount literals, compliance seize argument set, burn and seize CLI positional args). Keep examples runnable from repo-root conventions.</action>
  <verify><automated>rg -n "recipientTokenAccount|1_000_000n|compliance\\.seize\\(|burn <burnerTokenAccount> <amount>|seize <fromTokenAccount> <targetOwner> --to <treasuryTokenAccount>" README.md</automated></verify>
  <done>README examples are interface-accurate and executable in reviewer context.</done>
</task>

<task type="auto">
  <name>Task 2: Correct SDK doc signer and argument contracts with failure-path parity notes</name>
  <files>docs/SDK.md</files>
  <action>Ownership: SDK doc examples and notes only. Update lifecycle/compliance examples so signer keys and required args match current SDK APIs (including pause/unpause authority signer). Preserve prerequisite and error-envelope orientation; add concise failure-path note where mismatch risk is highest.</action>
  <verify><automated>rg -n "pause\\(\\{ authority \\}\\)|unpause\\(\\{ authority \\}\\)|recipientTokenAccount|targetOwner|treasuryTokenAccount|INVALID_ARGUMENT|MISSING_SIGNER" docs/SDK.md</automated></verify>
  <done>SDK documentation examples align with current API contracts and expected error semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh requirement traceability and phase verification verdict</name>
  <files>docs/TRACEABILITY.md, .planning/phases/09-documentation/09-VERIFICATION.md</files>
  <action>Ownership: traceability and verification docs only. Update DOC-02 traceability rows to reference corrected example sections, re-run CLI help surface checks, and rewrite the verification gap summary to mark prior example mismatches resolved with dated command/result evidence.</action>
  <verify><automated>./scripts/sss-token --help && ./scripts/sss-token mint --help && ./scripts/sss-token burn --help && ./scripts/sss-token seize --help && rg -n "DOC-02|README|docs/SDK.md" docs/TRACEABILITY.md && rg -n "status:\\s*(passed|complete)|DOC-02|resolved|Fresh Verification Evidence" .planning/phases/09-documentation/09-VERIFICATION.md</automated></verify>
  <done>Phase 9 verification report explicitly closes DOC-02 drift and keeps DOC-01/DOC-03 coverage intact.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "recipientTokenAccount|1_000_000n|compliance\.seize\(|burn <burnerTokenAccount> <amount>|seize <fromTokenAccount> <targetOwner> --to <treasuryTokenAccount>" README.md`
- [ ] `rg -n "pause\(\{ authority \}\)|unpause\(\{ authority \}\)|recipientTokenAccount|targetOwner|treasuryTokenAccount" docs/SDK.md`
- [ ] `./scripts/sss-token --help && ./scripts/sss-token mint --help && ./scripts/sss-token burn --help && ./scripts/sss-token seize --help`
- [ ] `rg -n "DOC-01|DOC-02|DOC-03" docs/TRACEABILITY.md .planning/phases/09-documentation/09-VERIFICATION.md`
- [ ] `.planning/phases/09-documentation/09-VERIFICATION.md` no longer reports open example drift failures
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 9 documentation examples are interface-accurate and verification no longer reports DOC-02 drift
</success_criteria>

<output>
After completion, create .planning/phases/09-documentation/09-06-SUMMARY.md
</output>
