---
plan: 02
type: execute
phase: 10-devnet-proof
title: Deploy Required Programs And Publish Canonical IDs
status: planned
wave: 2
depends_on: [10-01]
requirements: [DEP-01, DEP-02, DEP-03]
files_modified:
  - Anchor.toml
  - docs/SSS-1.md
  - docs/SSS-2.md
  - artifacts/devnet/phase-10/deploy/<RUN_ID>/manifest.json
  - artifacts/devnet/phase-10/deploy/<RUN_ID>/summary.md
autonomous: true
must_haves:
  truths:
    - DEP-01 is complete only when `sss_1`, `sss_2`, and `sss_transfer_hook` are deployed on devnet and canonical IDs are recorded.
    - Canonical IDs must be identical across Anchor config, docs tables, and deployment manifest outputs.
    - Program authority snapshots and deployment signatures are mandatory proof for each canonical ID.
  artifacts:
    - Anchor.toml
    - docs/SSS-1.md
    - docs/SSS-2.md
    - artifacts/devnet/phase-10/deploy/<RUN_ID>/manifest.json
    - artifacts/devnet/phase-10/deploy/<RUN_ID>/summary.md
  key_links:
    - DEP-01 deployment outputs -> Anchor.toml [programs.devnet] -> reviewer docs canonical ID tables
    - DEP-03 machine proof -> deployment manifest JSON -> explorer address/tx links + authority snapshots
---

<objective>
Deploy all required programs to devnet and lock a final canonical identity set.

Purpose: satisfy DEP-01 with reproducible, auditable deployment evidence.
Output: canonical devnet program IDs published in config/docs with deploy manifest and authority proof.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-devnet-proof/10-CONTEXT.md
@.planning/phases/10-devnet-proof/10-RESEARCH.md
@Anchor.toml
@docs/SSS-1.md
@docs/SSS-2.md
@scripts/devnet/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute devnet deployment pipeline for all required programs</name>
  <files>artifacts/devnet/phase-10/deploy/&lt;RUN_ID&gt;/summary.md, artifacts/devnet/phase-10/deploy/&lt;RUN_ID&gt;/manifest.json</files>
  <action>Ownership: deployment run artifacts only. Run build/deploy for `sss_1`, `sss_2`, `sss_transfer_hook`; capture deployment signatures, deployer identity, slot/time metadata, and per-program authority snapshots in deterministic Phase 10 deploy artifact paths. Keep the full deploy chain as a plan-level verification gate, not the per-task smoke check.</action>
  <verify><automated>anchor --version && solana --version && solana config get && rg -n "\\[programs\\.devnet\\]" Anchor.toml</automated></verify>
  <done>All required programs are deployed with captured signature and authority evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Publish canonical IDs in Anchor and reviewer-facing docs</name>
  <files>Anchor.toml, docs/SSS-1.md, docs/SSS-2.md</files>
  <action>Ownership: canonical ID publication surfaces only. Update `[programs.devnet]` with final IDs, then mirror the exact values in SSS docs canonical devnet ID tables with deployment timestamp reference and artifact link path.</action>
  <verify><automated>rg -n "\\[programs\\.devnet\\]|sss_1|sss_2|sss_transfer_hook" Anchor.toml && rg -n "Devnet Program ID|sss_1|sss_2|sss_transfer_hook" docs/SSS-1.md docs/SSS-2.md</automated></verify>
  <done>Canonical IDs are published and reviewer discoverable from docs and config.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce canonical ID equality gate across config/docs/manifest</name>
  <files>artifacts/devnet/phase-10/deploy/&lt;RUN_ID&gt;/manifest.json</files>
  <action>Ownership: deployment manifest validation fields only. Add explicit equality-check section showing IDs from Anchor/docs/manifest and fail status if any drift is detected; include explorer URLs for each program ID and deploy transaction signature.</action>
  <verify><automated>test -f artifacts/devnet/phase-10/deploy/<RUN_ID>/manifest.json && rg -n "\"canonicalPrograms\"|\"authoritySnapshots\"|\"explorerUrl\"|\"idEquality\"" artifacts/devnet/phase-10/deploy/<RUN_ID>/manifest.json</automated></verify>
  <done>DEP-01 identity proof is machine-verifiable and drift-protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `anchor build`
- [ ] `anchor deploy --provider.cluster devnet --program-name sss_1`
- [ ] `anchor deploy --provider.cluster devnet --program-name sss_2`
- [ ] `anchor deploy --provider.cluster devnet --program-name sss_transfer_hook`
- [ ] `solana program show <SSS1_PROGRAM_ID> --url devnet`
- [ ] `solana program show <SSS2_PROGRAM_ID> --url devnet`
- [ ] `solana program show <HOOK_PROGRAM_ID> --url devnet`
- [ ] `rg -n "\[programs\.devnet\]|sss_1|sss_2|sss_transfer_hook" Anchor.toml docs/SSS-1.md docs/SSS-2.md`
- [ ] `test -f artifacts/devnet/phase-10/deploy/<RUN_ID>/summary.md`
- [ ] `test -f artifacts/devnet/phase-10/deploy/<RUN_ID>/manifest.json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- DEP-01 has canonical devnet identity evidence with authority snapshots and explorer links
</success_criteria>

<output>
After completion, create .planning/phases/10-devnet-proof/10-02-SUMMARY.md
</output>
