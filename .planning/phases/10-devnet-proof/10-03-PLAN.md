---
plan: 03
type: execute
phase: 10-devnet-proof
title: Implement Deterministic Phase-10 SSS-1 And SSS-2 Proof Lanes
status: planned
wave: 2
depends_on: [10-01]
requirements: [DEP-01, DEP-02, DEP-03]
files_modified:
  - scripts/devnet/phase-10-sss1-proof.sh
  - scripts/devnet/phase-10-sss2-proof.sh
  - scripts/devnet/README.md
autonomous: true
must_haves:
  truths:
    - DEP-02 requires successful representative transactions for both SSS-1 and SSS-2 lanes on devnet.
    - Each lane must include one deterministic negative-path transaction with explicit expected-failure evidence.
    - Proof scripts must remain non-destructive and reject reused `RUN_ID` artifact directories.
  artifacts:
    - scripts/devnet/phase-10-sss1-proof.sh
    - scripts/devnet/phase-10-sss2-proof.sh
    - scripts/devnet/README.md
  key_links:
    - DEP-02 representative lane execution -> phase-10 scripts -> signatures/state snapshots/negative-path evidence
    - DEP-03 artifact verifiability -> stable script output schemas -> later manifest aggregation and reviewer package publication
---

<objective>
Create executable Phase 10 proof lanes for SSS-1 and SSS-2 using deterministic artifact contracts.

Purpose: prepare lane tooling needed to generate reproducible representative transaction evidence.
Output: production-ready Phase 10 proof scripts with strict output schema and failure semantics.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-devnet-proof/10-CONTEXT.md
@.planning/phases/10-devnet-proof/10-RESEARCH.md
@scripts/devnet/phase-08-sss1-proof.sh
@scripts/devnet/phase-08-sss2-proof.sh
@scripts/devnet/README.md
@scripts/sss-token
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SSS-1 Phase 10 lane with deterministic negative-path capture</name>
  <files>scripts/devnet/phase-10-sss1-proof.sh</files>
  <action>Ownership: SSS-1 Phase 10 script only. Implement representative SSS-1 lane using `./scripts/sss-token` with deterministic artifact outputs (`commands`, `signatures.csv`, state snapshots, summary metadata), plus one required negative-path operation and explicit expected-failure capture.</action>
  <verify><automated>bash -n scripts/devnet/phase-10-sss1-proof.sh && ./scripts/sss-token --help && ./scripts/sss-token init --help && ./scripts/sss-token mint --help</automated></verify>
  <done>SSS-1 lane is executable, deterministic, and captures both success and negative-path evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SSS-2 Phase 10 lane with compliance negative-path evidence</name>
  <files>scripts/devnet/phase-10-sss2-proof.sh</files>
  <action>Ownership: SSS-2 Phase 10 script only. Implement representative SSS-2 lifecycle and compliance lane (blacklist/seize included) with deterministic artifacts and one required compliance-oriented negative-path transaction captured as expected failure.</action>
  <verify><automated>bash -n scripts/devnet/phase-10-sss2-proof.sh && ./scripts/sss-token blacklist --help && ./scripts/sss-token seize --help</automated></verify>
  <done>SSS-2 lane is executable, deterministic, and captures compliance-path plus negative-path proof.</done>
</task>

<task type="auto">
  <name>Task 3: Document exact script interfaces and output schema in devnet README</name>
  <files>scripts/devnet/README.md</files>
  <action>Ownership: devnet README script section only. Add exact invocation examples for Phase 10 lane scripts, enumerate required env vars/flags, and lock per-lane output file contract consumed by publication manifests.</action>
  <verify><automated>rg -n "phase-10-sss1-proof|phase-10-sss2-proof|signatures\\.csv|summary\\.md|run-metadata\\.env|negative-path" scripts/devnet/README.md</automated></verify>
  <done>Phase 10 lane interfaces and artifact schema are documented and execution-safe.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/devnet/phase-10-sss1-proof.sh`
- [ ] `bash -n scripts/devnet/phase-10-sss2-proof.sh`
- [ ] `./scripts/sss-token --help`
- [ ] `./scripts/sss-token init --help`
- [ ] `./scripts/sss-token mint --help`
- [ ] `./scripts/sss-token blacklist --help`
- [ ] `./scripts/sss-token seize --help`
- [ ] `rg -n "negative-path|expected failure|signatures\\.csv|summary\\.md" scripts/devnet/phase-10-sss1-proof.sh scripts/devnet/phase-10-sss2-proof.sh scripts/devnet/README.md`
- [ ] Phase 10 lane scripts fail fast on reused artifact directories
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- DEP-02 proof lanes are executable and deterministic for SSS-1 and SSS-2
</success_criteria>

<output>
After completion, create .planning/phases/10-devnet-proof/10-03-SUMMARY.md
</output>
