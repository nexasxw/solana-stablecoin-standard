---
plan: 01
type: execute
phase: 11-docker-packaging
title: Docker Compose Packaging Baseline And Env Contract
status: planned
wave: 1
depends_on: []
requirements: [OPS-01, OPS-02]
files_modified:
  - services/mint-burn/Dockerfile
  - services/indexer/Dockerfile
  - services/compliance/Dockerfile
  - services/webhook/Dockerfile
  - .dockerignore
  - .env.example
  - docker-compose.yml
autonomous: true
must_haves:
  truths:
    - Root-level Docker Compose remains the canonical reviewer entrypoint for both default and sss2 flows.
    - All service images are buildable from repository source via Compose without wrapper scripts.
    - Reviewer environment contract is explicit, versioned, and copyable from root `.env.example`.
  artifacts:
    - services/mint-burn/Dockerfile
    - services/indexer/Dockerfile
    - services/compliance/Dockerfile
    - services/webhook/Dockerfile
    - .env.example
    - docker-compose.yml
  key_links:
    - OPS-01 top-level docker entrypoint -> docker-compose service build graph + root env contract.
    - OPS-02 profile-based service packaging -> default stack and sss2 compliance path both buildable.
---

<objective>
Establish deterministic Docker packaging primitives for the backend stack.

Purpose: make root Compose startup executable by ensuring all service build contexts and environment contracts exist.
Output: service Dockerfiles, root `.env.example`, and Compose alignment needed for profile-aware image builds.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-docker-packaging/11-CONTEXT.md
@.planning/phases/11-docker-packaging/11-RESEARCH.md
@.planning/phases/11-docker-packaging/11-VALIDATION.md
@docker-compose.yml
@services/mint-burn/package.json
@services/indexer/package.json
@services/compliance/package.json
@services/webhook/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic Dockerfiles for all runtime services</name>
  <files>services/mint-burn/Dockerfile, services/indexer/Dockerfile, services/compliance/Dockerfile, services/webhook/Dockerfile, .dockerignore</files>
  <action>Ownership: Docker build artifacts only. Create multi-stage Node 20 service Dockerfiles with dependency install, TypeScript build, and runtime command contracts that do not depend on local host tooling.</action>
  <verify><automated>test -f services/mint-burn/Dockerfile && test -f services/indexer/Dockerfile && test -f services/compliance/Dockerfile && test -f services/webhook/Dockerfile && test -f .dockerignore</automated></verify>
  <done>Compose build contexts are fully defined for all default and sss2 services.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce canonical reviewer environment contract</name>
  <files>.env.example</files>
  <action>Ownership: root environment template only. Add required variables for Docker runtime with safe placeholders/defaults, including RPC/WS/program IDs and service dependencies used by Compose.</action>
  <verify><automated>rg -n "RPC_URL|WS_URL|SSS1_PROGRAM_ID|SSS2_PROGRAM_ID|AUTHORITY_KEYPAIR|DATABASE_URL|REDIS_URL" .env.example</automated></verify>
  <done>Reviewers have a single root env template that matches Docker runtime expectations.</done>
</task>

<task type="auto">
  <name>Task 3: Align Compose build graph with service Docker contracts</name>
  <files>docker-compose.yml</files>
  <action>Ownership: compose manifest only. Ensure build directives, profiles, dependency ordering, and healthcheck prerequisites are consistent with the added Dockerfiles and env contract while preserving fixed service matrix.</action>
  <verify><automated>docker compose config >/dev/null && rg -n "mint-burn:|indexer:|webhook:|compliance:|build:|profiles:\\s*\\[\"sss2\"\\]" docker-compose.yml</automated></verify>
  <done>Top-level compose configuration resolves and all service images build successfully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose config >/dev/null`
- [ ] `docker compose build mint-burn indexer webhook`
- [ ] `docker compose build compliance`
- [ ] `test -f .env.example`
- [ ] `rg -n "profiles:\s*\[\"sss2\"\]" docker-compose.yml`
- [ ] `rg -n "Dockerfile" services/*/Dockerfile`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Compose build baseline is reproducible for default and sss2 packaging paths
</success_criteria>

<output>
After completion, create .planning/phases/11-docker-packaging/11-01-SUMMARY.md
</output>
