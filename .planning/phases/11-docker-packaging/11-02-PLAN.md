---
plan: 02
type: execute
phase: 11-docker-packaging
title: Runtime Health Readiness For Default And SSS-2 Profiles
status: planned
wave: 2
depends_on: [11-01]
requirements: [OPS-01, OPS-02]
files_modified:
  - services/mint-burn/src/index.ts
  - services/indexer/src/index.ts
  - services/compliance/src/index.ts
  - services/webhook/src/index.ts
  - services/mint-burn/package.json
  - services/indexer/package.json
  - services/compliance/package.json
  - services/webhook/package.json
  - docker-compose.yml
autonomous: true
must_haves:
  truths:
    - Default compose startup must bring up mint-burn, indexer, webhook, postgres, and redis with deterministic health success.
    - SSS-2 compose startup must additionally bring up compliance via `--profile sss2` with deterministic health success.
    - Runtime changes stay operational-only (startup + `/health`) and do not expand product API scope.
  artifacts:
    - services/mint-burn/src/index.ts
    - services/indexer/src/index.ts
    - services/compliance/src/index.ts
    - services/webhook/src/index.ts
    - docker-compose.yml
  key_links:
    - OPS-01 compose up contract -> healthy default stack and process liveness.
    - OPS-02 profile matrix contract -> compliance service healthy only when sss2 profile is enabled.
---

<objective>
Make Compose services actually become healthy under the locked Phase 11 startup contract.

Purpose: deliver minimal runtime entrypoints and health endpoints required by Compose checks.
Output: health-capable service runtime behavior verified for default and sss2 profiles.
</objective>

<context>
@.planning/phases/11-docker-packaging/11-CONTEXT.md
@.planning/phases/11-docker-packaging/11-RESEARCH.md
@.planning/phases/11-docker-packaging/11-VALIDATION.md
@docker-compose.yml
@services/mint-burn/src/index.ts
@services/indexer/src/index.ts
@services/compliance/src/index.ts
@services/webhook/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement minimal HTTP runtime and `/health` for default-profile services</name>
  <files>services/mint-burn/src/index.ts, services/indexer/src/index.ts, services/webhook/src/index.ts</files>
  <action>Ownership: listed service entrypoints only. Ensure each service starts a long-running HTTP process on expected port and exposes deterministic `GET /health` JSON response for Compose healthchecks.</action>
  <verify><automated>docker compose config >/dev/null && rg -n "mint-burn:|indexer:|webhook:|healthcheck:|3001|3002|3003" docker-compose.yml && rg -n "app\\.get\\('/health'|listen\\(" services/mint-burn/src/index.ts services/indexer/src/index.ts services/webhook/src/index.ts</automated></verify>
  <done>Default profile services remain up and pass health endpoint checks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement minimal HTTP runtime and `/health` for compliance profile service</name>
  <files>services/compliance/src/index.ts</files>
  <action>Ownership: compliance entrypoint only. Ensure compliance service boots in container mode and serves deterministic `GET /health` on port 3004 when `sss2` profile is enabled.</action>
  <verify><automated>docker compose config >/dev/null && rg -n "compliance:|profiles:\\s*\\[\"sss2\"\\]|healthcheck:|3004" docker-compose.yml && rg -n "app\\.get\\('/health'|listen\\(" services/compliance/src/index.ts</automated></verify>
  <done>Compliance service becomes healthy only in sss2 profile startup path.</done>
</task>

<task type="auto">
  <name>Task 3: Wire package runtime scripts and compose command contracts for deterministic startup</name>
  <files>services/mint-burn/package.json, services/indexer/package.json, services/compliance/package.json, services/webhook/package.json, docker-compose.yml</files>
  <action>Ownership: listed package manifests and compose file only. Align `start` command paths, runtime env usage, and compose command expectations so containers run built entrypoints consistently.</action>
  <verify><automated>docker compose config >/dev/null && rg -n "\"start\"\\s*:" services/mint-burn/package.json services/indexer/package.json services/compliance/package.json services/webhook/package.json && rg -n "mint-burn:|indexer:|webhook:|compliance:|command:|profiles:\\s*\\[\"sss2\"\\]" docker-compose.yml</automated></verify>
  <done>Both profile paths run with deterministic startup and no broken entry command mismatches.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose down -v || true`
- [ ] `docker compose --profile sss2 down -v || true`
- [ ] `docker compose build`
- [ ] `docker compose up -d && docker compose ps`
- [ ] `curl -fsS http://localhost:3001/health`
- [ ] `curl -fsS http://localhost:3002/health`
- [ ] `curl -fsS http://localhost:3003/health`
- [ ] `docker compose --profile sss2 up -d && docker compose --profile sss2 ps`
- [ ] `curl -fsS http://localhost:3004/health`
- [ ] `docker compose logs --tail=80 mint-burn indexer webhook compliance`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Compose startup and health gates are deterministic for default and sss2 profiles
</success_criteria>

<output>
After completion, create .planning/phases/11-docker-packaging/11-02-SUMMARY.md
</output>
