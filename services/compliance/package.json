{
  "name": "@stbr/sss-compliance",
  "version": "0.1.0",
  "private": true,
  "description": "SSS-2 blacklist management and audit trail service",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "test": "mocha -r ts-node/register --timeout 10000 --exit 'src/__tests__/**/*.test.ts'",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.3",
    "ts-node": "^10.9.2"
  }
}
