{
  "name": "@stbr/sss-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared backend contracts and persistence primitives for SSS services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "test": "echo 'No tests yet' && exit 0",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.3",
    "ts-node": "^10.9.2"
  }
}
